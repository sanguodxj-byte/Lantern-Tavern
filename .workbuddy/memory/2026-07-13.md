# 2026-07-13

## 移除 GitNexus，改用自研 GDScript 静态索引器
- 用户最初要求给本 Godot/GDScript 项目部署 GitNexus(MCP 代码知识图谱)。但 GitNexus 基于 Tree-sitter，只支持 TS/JS/Python/C#/Go/Rust/C/C++/PHP/Swift/Dart 等，**不含 GDScript**；本项目 99% 是 .gd，故无意义。用户随后叫停："那没必要了，移除，使用其他方案"。
- 已撤销：从 `~/.workbuddy/mcp.json` 移除 gitnexus 条目（保留 blockbench-ashfox）；kill 后台 `gitnexus analyze`(跑了约 4 分钟)；删除家目录 `~/.gitnexus/`(含更早会话的 The-Second-Seat / CharacterStudio 旧索引)。项目内无 `.gitnexus` 残留。
- 替代方案：新建 `tools/gdscript_codemap.py`(纯 stdlib，无外部依赖)，静态解析 GDScript 的 class_name / extends / preload("res://") / signal / enum / const / var(@export/@onready) / func(@rpc/@static) / 内部 class，构建「继承 + 文件依赖 + autoload 使用」图谱。
- 产物：`docs/gdscript_codemap.json`(机器可读图) + `docs/CODEMAP.md`(人类报告：autoload 注册表 / 类继承 / 被依赖最多 / 体量最大 / autoload 使用)。运行：`python tools/gdscript_codemap.py . --out-json docs/gdscript_codemap.json --out-md docs/CODEMAP.md`(默认排除 addons/，可加 `--include-addons`)。
- 首次运行：486 个 .gd，140 class_name，5820 func(9 @rpc)，依赖边 814→911，30 个 autoload(含 4 个 uid:// 单例 GameState/GameEvents/HitStopServer/FxHelper，正则须同时吃 res:// 与 uid://)。`GameState` 在 45 文件被引用。
- 注：脚本为 Python dev 工具、非游戏 GDScript，故不强制 gdUnit4 单测(AGENTS.md 测试硬规则针对 .gd 游戏代码)。

## ⑫ 无头专用服务器（Dedicated Server）—— 完成并验证 PASS
- 实现 `scenes/multiplayer/dedicated_server.gd` + `.tscn`(无 class_name，autoload NetworkManager 已就绪)。与 listen-server(host 也是玩家)本质区别：专用服务器【本身非玩家】——不 spawn_self、不建可见地牢/本地 Player、不渲染；只 host + 常驻 MultiplayerSceneBridge(服务器侧转 avatar/snapshot) + 首个 peer 接入触发出征 + 每帧 tick(服务器时钟/快照节流/断线清理)。
- 出征：`NetworkManager.start_expedition()`(权威 seed→广播 dungeon_layout) + `DungeonSessionController.build_authority_only(seed)`(仅建权威 layout/出生点，跳过场景几何) + `spawn_server_entities()`。晚到 peer→`_replay_for_late_join`(同 MultiplayerSession._on_peer_authorized：重播 layout + rebroadcast_entities + 定向 SESSION_SNAPSHOT)。
- 参数：env `DS_PORT`/`DS_MAX_PLAYERS`/`DS_IDLE_SHUTDOWN_SEC` 优先，CLI `--port= --max-players= --idle-shutdown=` 覆盖；默认 54321/8/0(不自动关服)。
- 验证：`tools/run_dedicated_server_test.ps1` 双进程 ENet(专用服务器进程 + 真实客户端进程 ITEST_ROLE=client)。结果 `DEDICATED SERVER TEST: PASS`：专用服务器 READY、客户端 fp_match=true(同 seed 重建)、entities=2 复制到位、local_x=21.867 moved=true 且收 81 条 player_snapshot(server.log 0 SCRIPT ERROR)。修复两处实现坑：`pass` 是 GDScript 保留字(改名 ok_all)；.tscn 中 bridge 脚本须走 ext_resource id 而非内联 res:// 路径。
- 交付：`tools/dedicated_server.cmd`(真机部署启动器)；`tools/dedicated_srv.cmd`/`dedicated_cli.cmd`(测试进程启动器)。
- 联机六大件 ⑧⑨⑩⑪⑫ 全部 PASS。剩余可选架构债：DungeonSessionController 并入 procedural_dungeon.gd 入口统一(经 MultiplayerSession 已大部分消解)。

## 架构债收尾：统一出生点算法（玩家出生点单一真相源）
- 根因：玩家出生点数学在 `DungeonSessionController._compute_player_spawn_pos()` 与 `ProceduralDungeon._build_terrain_geometry()` 两处**各自复制**。管线(DungeonGenerator+DungeonSceneBuilder)早已共享，真正漂移源只有出生点公式。若某天 ProceduralDungeon 改算法，专用服务器权威出生点将与客户/单人不一致，破坏跨进程指纹与实体位置确定性。
- 修复：在 `DungeonLayout`(纯 RefCounted 共享布局契约)新增 `calc_player_spawn_pos() -> Vector3` 作为**唯一**算法来源；两端(真实 ProceduralDungeon + DungeonSessionController/build_authority_only)都经它计算。三分支(优先 player_spawn_cell → 回退 start 房间整数中心 `_rect_center_cell` 语义 → 原点 (0,0.5,0))已用 headless 验证与旧实现逐字节一致。
- 清理：`ProceduralDungeon._build_terrain_geometry` 现 4 行委托；删除其私有 `_rect_center_cell`(外部无引用)。**坑(已规避)**：曾误删 `ProceduralDungeon.TILE_SIZE`，但 `scenes/ui/minimap.gd` + 两个 gdUnit 测试经 `ProceduralDungeon.TILE_SIZE` 读取——勿删被外部代码引用的 const，已还原。
- 验证：两集成测试重跑全 PASS（DEDICATED SERVER TEST: PASS / INTEGRATION DUNGEON TEST: PASS；client fp_match=true、entities=2、movement 正常、server 0 SCRIPT ERROR）。架构债至此清零。

## Phase 1 完成：玩家权威输入边界（联机开发方案第一阶段）
- 用户给出 10 阶段联机开发方案（范围：地牢/出征/战斗/敌人掉落宝箱门出口/拾取装备丢弃/撤离结算/断线重连快照；不接酒馆经营）。当前从 Phase 1（修复权威边界）起步。
- `player.gd`：新增 `enum InputMode {LOCAL, NETWORK_CLIENT, NETWORK_SERVER}` + `var input_mode/multiplayer_driver` + `configure_network_input(driver,mode)` + `is_network_controlled() -> input_mode==NETWORK_CLIENT`。单机恒 LOCAL（is_network_controlled=false，行为不变）。
- `player.gd._physics_process`：联机客户端下，原本地 `chest.open_chest()` / `collider.interact()` / 直接改敌血 等权威操作被 `if is_network_controlled(): return` 门控（只保留本地预表现/选中高亮），意图改由 ClientCommandDriver 上送。
- `player_state_moving.gd._process`：联机客户端下，`use/throw/action/block/jump` 不再本地 transition 到 PICKING_UP/THROWING/ATTACK/...，改为调用 driver 的 `send_pickup/send_throw/send_attack/send_block/send_extract`；本地只做预表现与接收服务器事件切状态。
- `client_command_driver.gd`：扩展统一命令接口 `send_attack/send_block/send_skill/send_interact/send_pickup/send_throw/send_equip/send_drop/send_extract` + 既有 `send_input_frame`；`_ready` 自动 `player.configure_network_input(self, NETWORK_SERVER if is_host else NETWORK_CLIENT)`；CMD_INPUT 增 `block` 字段。
- 验证：headless parse 全过(exit=0)；`DEDICATED SERVER TEST: PASS`(local_x=21.733 moved, 82 snapshots, fp_match) + `INTEGRATION DUNGEON TEST: PASS`(combat/loot/extract/recovery 全绿) —— Phase 1 零回归。
- 注：GDScript(Script 资源)无 `has_method` 实例语义，lean parse check 中 `P.has_method(...)` 返回 false 是假阴性，实际方法已 grep 确认存在(lines 53-79)。

## Phase 2 完成：补齐网络命令 + 集中式反作弊守卫
- 探索发现方案假设的多数命令(INTERACT/PICKUP/ATTACK/INPUT/EXPEDITION/REQUEST_LAYOUT/EXTRACT/RESUME)在 session_root.wire_default_authorities 已注册；缺 CMD_SKILL/EQUIP/DROP/SAVE/LEAVE 处理 + 统一「禁止客户端传入并被信任」字段守卫。
- `command_validator.gd`：新增 `FORBIDDEN_TRUSTED_FIELDS := ["position","velocity","damage","current_life","inventory_delta","drop_amount","weapon_stats","player_attributes","save_state"]` 及 `validate_no_trusted_fields(req)` 守卫（任何携带即 PERMISSION_DENIED，根防 GLITCH/秒杀/无限资源）。
- `session_root.gd`：`on_command` 在分发前先查 forbidden 字段；`wire_default_authorities` 注册并实现 `_handle_skill/_handle_equip/_handle_drop/_handle_save/_handle_leave`（服务器权威、sequence 重放保护、夹紧持有量、校验拥有权、leave 清理 live_state/registry/entities）。
- 单测：`command_validator_test.gd` 加 forbidden 守卫用例；`session_root_test.gd` 加 forbidden 拒绝 + 5 处理器行为用例（drop 夹紧、equip/skill 拥有权、save 返回结算、leave 清理）。
- 验证：两文件 headless 全 PASS（command_validator 0 错；session_root 45 用例 0 错 0 失败）。两集成测试(DEDICATED/INTEGRATION)重跑零回归。

## Phase 3 完成：战斗权威目标/姿态/状态校验 + 出生点接线 bug 修复
- `combat_authority.gd`：新增 `validate_attack_targeting(attacker, target, cfg) -> String`，校验 ①冷却(服务器绝对时间 `_attack_cd_until`，客户端无法绕过连点) ②硬直(`_stagger`) ③射程(`_attack_range_for` 基于激活武器，远程须确持武器否则降级近战) ④扇区(攻方 facing vs 目标方向 dot≥`ATTACK_SECTOR_HALF_COS=cos75°≈0.259`，防背后命中) ⑤视线(`los_ok`，由服务器真实几何算)；无目标时按 `allow_missing_target` 决定是否拒(空挥锁定类攻击拒 ERR_INVALID_TARGET)。另 `validate_weapon_ownership` 静态守卫。
- `session_root.gd`：`_handle_combat` 接 `validate_attack_targeting`（快照全取服务器权威：位置来自 live_state/实体注册表、facing 来自积分的 look_yaw、冷却来自当前时间）；结算成功写 `_attack_cd_until[peer_id]=current_time+SERVER_ATTACK_CD(0.4s)`。live_state 加 `facing/look_yaw` 默认；`network_protocol.gd` 加 `ERR_ATTACK_NOT_FACING`。
- 坑(已修)：`_handle_combat` 内 `defender_entity_id` 被 `var` 声明两次 → GDScript 同名变量重复声明 Parse Error；删 resolve 块重复声明复用上方变量即解。
- 单测：`combat_authority_test.gd` 加 9 个 Phase 3 用例(冷却/硬直/超出射程/未朝向/视线遮挡/空挥拒/合法通过/位置未知降级跳过)，headless 全 PASS。三套 gdUnit(session_root 45 / command_validator 10 / combat_authority 12 用例) 0 错 0 失败。
- **朝向权威化修正（重要）**：原 `_handle_movement` 把 `command.look_yaw`（客户端自报回声）存为 `live.facing`。但真实玩法里没有任何本地系统根据瞄准/移动设置 `player.rotation.y`——仅由 `apply_remote_snapshot` 从服务器快照回声写回，形成恒为 0(朝 -Z)的回声环，扇区校验会误拒所有非 -Z 方向攻击。改为：用服务器自己积分出的**实际位移方向** `atan2(Δx, -Δz)` 推导 `yaw→facing`，不可伪造，符合方案「用实际朝向、不信任客户端自报」。
- **关键根因 bug（Phase 3 激活几何校验后暴露）**：`session_root.handle_spawn_request` 把玩家权威出生点注册成 `Vector3.ZERO`，而敌人按 `DungeonLayout.calc_player_spawn_pos()`（地牢出生点，如 (-42,-24)）生成 → 玩家与敌人隔 40m，射程校验正确拒掉所有攻击。集成测试 `combat_ok=false` 即此因（旧版无射程校验掩盖）。
  - 修复：`session_root` 新增 `var player_spawn_pos: Vector3 = Vector3.ZERO`（默认保留以便 headless 单测无场景依赖）；`handle_spawn_request` 改用 `player_spawn_pos` 签发权威起始位置。`dungeon_session_controller._compute_player_spawn_pos()`（build_authority_only + 真实地牢两条路径都走）在算出 `_player_spawn_pos` 后写 `NetworkManager.session.player_spawn_pos = _player_spawn_pos`，并对**已在生成地牢前 spawn_self 的房主** `set_player_position(local_peer_id, _player_spawn_pos)` rebasing，避免房主被自身快照拉回原点。
- **集成测试改造（确定性）**：`mp_dungeon_test.gd` 战斗循环原 `override_move=Vector2(1,0)` 持续 +X 会冲过敌人(overshoot)；改为朝 `TARGET_ENEMY(1001)` 移动、进入 1.5m 即 `override_move=ZERO` 停步持续挥砍（真实玩家也停在敌前）。移动断言(断言2+3)原判 `abs(x)>0.5` 在出生点 x=-42 时 600 帧内到不了 → 改为「相对出生点位移 delta>0.5」判定，对出生点坐标鲁棒。
- 验证：两集成测试均 PASS —— `INTEGRATION DUNGEON TEST: PASS`(COMBAT combat_ok=true / MOVEMENT OK / ENTITIES/LOOT/EXTRACT/RECOVERY 全绿)；`DEDICATED SERVER TEST: PASS`(client local_x=22 moved, fp_match, entities=2)。
- **顺手清除专用服务器 SCRIPT ERROR 致其 verdict 假 FAIL**：`run_dedicated_server_test.ps1` 对 server.log 中 `SCRIPT ERROR` 计数门禁(`$SERVER_ERR -eq 0`)，而 `player_state_shooting.gd:72` `var muzzle_transform := player.view_model.get_muzzle_global_transform()` 因返回类型未静态标注致「Cannot infer type」Parse Error，连带 5 条「Failed to compile depended scripts」(player.gd 等依赖者) → 6 条 SCRIPT ERROR 使该测试被判 FAIL（地牢集成测试 .ps1 不门禁 SERVER_ERR 故一直 PASS）。`view_model.gd:144` 确认该方法返回 `Transform3D`，遂改为 `var muzzle_transform: Transform3D = ...` 显式标注，6 条错误全清 → `DEDICATED SERVER TEST: PASS`。此错误为 Phase 3 之前既存、与联机权威无关，修复属项目健康度改善。
- 状态：Phase 1/2/3 全完成且零回归；联机六大件 ⑧⑨⑩⑪⑫ + Phase1/2/3 集成验证全绿。下一步 Phase 4（peer_guid 重连模型，已知 D1 重连依赖旧 peer_id 是真实 bug）。

## Phase 4 完成：peer_guid + reconnect_token 稳定重连模型（D1 bug 真修复）
- 探查结论：设计文档(§13/§14.2)早规划了 player_guid/reconnect_token，但代码里 player_guid 只被存储、**从不参与任何查找**；重连链路全程用会变的 peer_id 作主键，入口即失败；且无任何客户端真正发起 send_resume()、无测试覆盖真实重连（单测全用固定 peer_id=1 掩盖 D1）。client2 集成段实为「晚到加入」非「断线重连」。
- 核心修复（身份锚定不依赖 peer_id）：
  - `connection_authority.gd`：新增 `_guid_to_peer: Dictionary`（guid→当前 peer_id 稳定索引）；加 `find_grace_peer_by_guid(guid)` / `find_grace_peer_by_token(token)`（token 全局唯一作兜底锚定）/ `validate_reconnect_by_guid(guid,token,now)` / `migrate_peer(old,new)`（把 GRACE 条目整条从旧 peer_id 迁到新 peer_id，同步 _guid_to_peer）。
  - `session_root.gd`：`on_command` 对 `CMD_RESUME` 提前短路（跳过在线态/live_state/ctx 检查，因新 peer_id 迁移前无这些）；`_handle_resume(command,_ctx)` 改从 command 读 `peer_id/token/player_guid`，先 `validate_reconnect_by_guid` 反查旧条目 → `connection_auth.migrate_peer(old,new)` + `_migrate_peer_state(old,new)`（把 registry/PlayerContext/live_state/位置朝向/出征基线/攻击冷却/硬直/interaction 锁整体接管，保留 inventory 与位置）→ `connection_auth.resume(new,...)` → 重置 sequence → 下发 SESSION_SNAPSHOT。
  - `network_manager.gd`：客户端缓存 `reconnect_player_guid`（send_spawn/spawn_self 时存）；`send_resume()` 携 `(token, guid)` 上送；`rpc_client_resume` + `_server_handle_resume` 增 guid 参数。
  - `player_context.gd`：加 `player_guid` 字段（`for_peer` 末参注入，可追溯性）。
  - `interaction_authority.gd`：加 `migrate_locks(old,new)`（重连时交互锁改挂新 peer_id）。
- 测试：connection_authority_test 加 6 用例(guid/token 反查/校验/迁移)、session_root_test 加 2 用例(新 peer_id 迁移保 inventory+位置+不串号)、network_manager_integration_test 加 1 用例(经真实 `_server_handle_resume` 入口、新 peer_id 接管)。既有的 `test_resume_with_wrong_token_rejected` 补 player_guid 以命中 TOKEN_INVALID(否则按 guid 缺失回落到 PEER_UNKNOWN)。
- 验证：5 套 gdUnit 全绿（connection_authority 29 / session_root 47 / network_manager 9 / combat_authority / command_validator，0 错 0 失败）；`INTEGRATION DUNGEON TEST: PASS`(combat/loot/extract/recovery 全绿) + `DEDICATED SERVER TEST: PASS` —— Phase 4 零回归。
- **坑(已修)**：`handle_spawn_request` 改动时把 `for_peer(...,guid)` 放在 `var guid` 声明之前 → GDScript「Identifier "guid" not declared」Parse Error；把 `var guid` 提到 `for_peer` 之前即解。该 Parse Error 会让整个项目 headless 编译失败（集成测试必崩），务必先声明后使用。
- 状态：Phase 1~4 全完成零回归。D1(重连依赖 peer_id) 已根除。下一步 Phase 5（存档信任边界）/ ⑥ 心跳 / ⑧ lobby 生产化 / 实体同步测试分层。

