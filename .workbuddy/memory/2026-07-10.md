# 2026-07-10 工作日志

## 文档21 拆分完美格挡精通为两个独立技能（用户纠正）
- 用户纠正："完美格挡精通 是俩个不同技能"。原doc21把窗口延长+增伤合并成单技能 `perfect_block_mastery` 是错的。
- doc21 拆分：#5 完美格挡·窗口延长 `perfect_block_window`（0.3s→0.8s，持续改判定窗口，非触发型） / #6 完美格挡·增伤 `perfect_block_empower`（完美格挡后下次攻击+X%，触发型buff标记，仅生效一次）。原#6-#8残影/取消后摇/自动拾取顺延为#7-#9。
- 同步更新：§3.2 callout(#5/#6)、§3.3 schema枚举、§4 B类符文映射(拆两行)、§5 解锁(窗口延长=T2增伤=T3)、§7 待决 D-P5/D-P6 各自绑定单技能 + 新增 D-P7(两技能可否同时装备占两槽)。
- 关键设计点：窗口延长=持续生效改判定窗口(无"本次/下次"语义)；增伤=触发型buff标记仅生效一次。二者共享触发点(完美格挡成功)但互不冲突。
- 全文已无 `perfect_block_mastery` / "完美格挡精通" 残留引用。

## 文档21 §9 后续扩展方向（整合散落被动体系）
- 用户问"看看后续扩展方向"。读 doc15/99/05/19/20 后，发现大量被动散落未纳入doc21统一框架。
- doc21 新增 §9：9.1 覆盖盘点；9.2 五大扩展轴——轴1体系整合(属性里程碑被动18条doc15§3.2 / 护甲熟练度特权轻甲重甲各1-3阶doc99-05最大空白 / 技能内嵌被动Riposte·ManaSurge)；轴2增设第三类反应类reactive(事件总线,完美格挡增伤/侧垫步/击杀回血归并)；轴3资源经济类(target=RESOURCE子类型,耐力/HP/法力/换弹)；轴4协同组合被动(doc19/20/21联动闭环)；轴5扩展操作清单(aim/throw/roll/interact/reload/backstab/land)。9.3 优先级待办P0~P3。
- 关键发现：护甲熟练度特权(轻甲/重甲)整套未入任何文档=最大缺口；属性里程碑被动与doc21被动需澄清5槽归属避免重复。

## 新增投掷武器(飞刀) + 轻弩装弹机制（用户指令）
- 用户指令："添加投掷类型武器，并且给弩增加装弹机制，不允许连续发射"。
- 关键区分：doc06 已有 匕首/短刀(近战)，故新增投掷武器建模为 **飞刀 THROWN_DAGGER**（投掷类，有限弹药可拾回），与近战匕首区分；属"投掷类型武器"类，后续可扩展投矛/炸弹。
- 轻弩 LIGHT_CROSSBOW 固有机制 multishot(连射)→ **reload_shot(装弹射击)**：每次射击后须装弹(~1.2s,可打断重来)，装弹完成前不允许连续发射；命中带钉身。
- 跨文档改动：
  - doc20 §2 武器层加 飞刀行+改轻弩行；§3.3 总表加 飞刀+改轻弩；§5 B映射 multishot→reload_shot 并加 throw 行；§7 D-T1(11武器/回写weapons.json)、D-T4(加reload_shot/throw路由)、新增D-T7(装弹状态机代码新增)。
  - doc15 §2 加 K.飞刀流派(3技能:回手飞刀/扇刃连投/穿心投掷)；G轻弩加⚠️装弹硬规则注记+双发连射/齐射改为"动作内/burst后须装弹"；计数11大战斗流派36技能。
  - doc21 §2.2 轻弩"连弩校准cooldown"→"迅捷装填reload_time-20%"+加飞刀精通行；§3.2 加#10快速装弹(reload)/#11投掷回收(throw)；§3.3 schema op/enhance加reload/throw/quick_reload/throw_recover；§4/§5/§9.1/§9.2轴5同步。
  - doc06 装备表加 飞刀行(敏捷投掷,有限携带×8/12/16)；轻弩三阶"连续射出多支"→"装填后射一支须重装弹"。
  - doc05 §B 远程伤害类型加飞刀+轻弩装弹/飞刀弹药机制注记；远程物理攻击经验绑定加飞刀。
  - doc99 技能大表计数 10武器33→11武器36。

## 撤销"飞刀独立武器类型"，改为"投掷类被动"（用户二次纠正）
- 用户纠正：投掷动作是**角色共有动作**，不应新增独立武器类型（之前我做的飞刀 THROWN_DAGGER 武器类型/流派/装备行过度设计）。应改为在被动系统(doc21)添加"投掷类"被动技能。
- 回退跨文档改动（保留轻弩装弹机制 reload_shot）：
  - doc20: 删武器层飞刀行、§3.3合成表飞刀行、§5 B映射throw行；D-T1(11→10条目,去飞刀ammo)、D-T4(去throw路由)、D-T7(去飞刀弹药系统)、§1"11武器"→10武器×2处。
  - doc15: §2标题36→33、计数说明去飞刀、删 K.飞刀流派整节。
  - doc06: 删飞刀装备行(敏捷投掷×8/12/16)。
  - doc05: 远程物理伤害类型去飞刀、删飞刀弹药机制行、远程攻击绑定去飞刀。
  - doc99: 技能大表计数 11武器36→10武器33。
- doc21 落地"投掷类被动"（核心交付）：数值类"飞刀精通"→"投掷（共有动作）精通 damage_mult×1.15"；机制类#11"投掷回收 throw_recover"标注改"角色共有动作"、解锁改"投掷类T2(AGI≥10里程碑或投掷熟练度≥Lv8)"；§9轴5注记去飞刀。
- 设计结论：投掷=共有动作(非武器类型)，其强化走被动系统(数值+机制)，与轻弩装弹(reload_shot武器机制)正交。

## 投掷复用远程武器熟练度（用户指令"投掷复用其他武器的熟练度"）
- 用户指令"投掷复用其他武器的熟练度"。决议：投掷为共有动作，不独立设熟练度/经验线，**复用远程武器熟练度**（与长弓/轻弩同门槛：远程熟练度≥Lv8 且 主攻AGI≥35）。
- doc21 改动：§5 投掷回收throw_recover解锁改"复用远程武器熟练度"(武器阶梯-远程线)；§5注记扩写"投掷类被动熟练度复用"段；数值类"投掷精通"行加"解锁复用远程武器熟练度阶梯"；§7 新增D-P8(已决议)。
- doc05 改动：§B远程物理伤害类型标题加"投掷动作"；§B加"投掷动作归类"段(伤害归远程物理、复用修正/倍率、熟练度经验复用远程线)；远程经验表"远程物理攻击"绑定加"投掷动作"(复用远程线不单设)。
- 附带背景：doc15/doc19 的"抓取投掷/踢击/冲撞/战术滑步"是独立技能(impact/stun机制)，与本文"投掷共有动作"是两回事，不冲突。

## 设计文档跨文档缺陷审查（用户\"看看哪里还有缺陷\"）
- 审查 doc15/19/20/21/05/06/99 一致性，修正 4 处事实错误：
  - doc15 §2 标题\"11大/33个\"与正文10个流派节(A–J)不符 → 改\"10大/30个\"；doc99 同计数33→30。
  - doc21 §9.1 误称\"护甲特权尚未入任何文档/最大缺口\"——实际 doc05§装甲熟练度(228-235)/doc06§装备熟练度(85-92) 已完整定义轻盈步法/侧身闪避/疾风闪跃/装甲加固/冲击缓冲/不屈壁垒。纠正为\"内容已写、归属未定(未并5槽)\"。
  - doc21 §9.1 待决列漏 D-P8 → 补。
- 结构性缺陷(未决,交付审查报告)：
  - P0-D1：B类符文命名三套不统一(doc19§3.1正典 灼燃/蔓延/守护(Othala)/连锁/寒霜/生息/破晓 vs doc20§5自创锐利/疾行/冲击(Thurisaz)/蛮力/噬魂 vs doc21§4自创蓄能(Othala)/锐觉(Raidho)/宽限/蓄势...)，且 doc21§4 把 Raidho(ᚱ)用于完美格挡而doc19把Raidho给A类quick→跨A/B字形冲突。根因G9未完成。
  - P0-D3：5被动槽 vs 24被动(13数值+11机制)来源分配未决(doc20 D-T6/doc21 D-P1)。
  - P1-D4：数值被动stat键(stagger_sec/range_m/armor_pen/reload_time/block_value/block_stamina/element_infuse.dps/spell_echo.dmg_mult)未在doc19§5符文修饰键契约登记。
  - P1-D5：doc19§11.1\"被动=给武器特殊机制\"与doc21数值/机制二分过时。
  - P1-D6：投掷共有动作触发身份未定义(与\"抓取投掷\"技能边界)。
  - P1-D7：属性里程碑18被动与本文被动5槽归属未并。
  - P2-D8/D9/D10：完美格挡D-P5/6/7、cd_reduce归类D-P4、蓄力跨武器D-P2。
  - P3-D12：D-P3代码实名制(jump/sprint/dodge/block/throw/reload状态机名)。doc19 G1/G8/G3/G9 实现缺口仍开。
- 结论：先定D3(5槽总规则)→统一D1(B符文命名,G9)→建D4数值键表→D12代码对齐。

## 修正 P0：D1(B符文命名统一) + D3(5槽分配规则) — 已落地
- 用户指令"修正p0,问题是什么"。先诊断后修。
- D1：doc20§5自创蛮力/嗜血/冲击(Thurisaz)/崩裂/疾行/锐利/标记扩散/迅捷装填/钉身延长/余响/噬魂；doc21§4自创蓄能(Othala)/锐觉(Raidho)/油润/锐锋/宽限/过载/三段/速装/多重/连环/震荡/蓄势。硬伤：Raidho(ᚱ)被doc21用于急速但doc19已分给A类quick；Othala(ᛟ)被doc21挪用为蓄能但doc19规定=守护(被动向)。
- 修复D1：doc19§3.1 B类池升为正典池(9符:灼燃Fehu/蔓延Hagalaz/守护Othala/连锁Nauthiz/寒霜Isa/生息Berkano/破晓Dagaz/迅捷Ehwaz/回响Jera)，新增迅捷(Ehwaz)/回响(Jera)两符，glyph与A类零重复；doc20§5与doc21§4映射表全部改写为引用正典9名，自创名一律作废。
- D3：doc21新增§5.1——①来源配额(武器类2/属性里程碑1/护甲特权1/机制类1=5槽)；②预设槽(slot1=武器固有机制常驻,换武器即换)；③同op叠加(效果叠加/互斥标记取较强者)、完美格挡#5#6不互斥可同占两槽。doc20 D-T6、doc21 D-P1/D-P7 标已决议。
- 验证：grep自创B名无残留(剩余命中均为被动技能名如蛮力负荷/锐锋延伸/迅捷装填或作废清单)；Raidho仅A类、Othala仅=守护,字形冲突消除。
- 仍待P1：doc19§11.1概念过时、D4数值键登记表、D6投掷触发身份、D12代码实名制。

## 统一冲突修正（用户"进行冲突修正，同一进行"）
- 一次性收掉 P1 四处文档冲突：
  - **doc19§11.1 概念过时**：旧写"被动技能=给武器特殊机制"与doc21数值/机制二分矛盾。改写为三层模型——被动=build骨架(分数值类/机制类,数值类非机制)、符文=放大/升阶、主动+装备=执行层；明确旧稿已过期。
  - **D4 数值stat键契约+键冲突**：doc21§2.1 schema删错置的cooldown键(归机制类cd_reduce)、补全键全集；新增§2.3数值被动stat键登记表(10键,域/是否被符文复用/聚合规则)，确立"被动写基础面板meta层、符文写技能有效值run层,同一键不双写,链式聚合"规则,解决damage_mult双写风险；doc19§5加交叉引用。
  - **D6 投掷边界**：doc21§3.2加注记——投掷共有动作=角色底层通用动作(基础层),抓取投掷=其上一个主动技能实例;throw_recover作用于基础层覆盖抓取投掷。新增D-P9(已决议)。
  - **D-P3 代码实名制**：doc21新增§3.4操作→代码对齐参考表(9个op的预期输入/状态机钩子),标注待player.gd等代码核实;D-P3改"已补参考表待代码核实"。
- 校验：grep确认"给武器特殊机制"仅留作过期批注、cooldown已移出数值schema、§2.3/§3.4/D-P9/分层聚合均落地。
- 仍未决(非冲突类,设计拍板):D-P2(机制类跨武器共享)、D-P4(cd_reduce归类)、D-P5(窗口延长是否同时持盾&双手)、D-P6(增伤X%与失效)。

## 用户拍板 D-P2/D-P5（"蓄力为全近战,完美格挡同时作用"）
- 用户指令"蓄力为全近战,完美格挡同时作用,其他争议具体是什么"。
- D-P2 已决议:蓄力 `charge` 为**全近战武器通用**机制(单手剑/双手大剑/双手斧/战锤/长枪),非单手剑独占;doc21 §3.2#1加"全近战通用"注、§5解锁改"全近战 T2"、§7 D-P2标已定。
- D-P5 已决议:完美格挡·窗口延长 `perfect_block_window` 的0.8s窗口**同时作用于持盾完美格挡与双手武器完美格挡**(均0.3→0.8);doc21 §3.2#5加"同时作用"注、§4破晓(Dagaz)行删"双手优先"、§7 D-P5标已定。
- 剩余未决(用户问的"其他争议"):**D-P4**(cd_reduce归类—数值感强是否划数值类)与**D-P6**(完美格挡·增伤#6的+X%数值与buff失效规则:切武器/超时/非攻击动作是否保留、是否严格仅下一击、连续完美格挡是否刷新不叠加、B符文连锁自动反击是否吃+X%)。D-P3为代码核实任务(非争议)。

## 用户拍板 D-P4/D-P6（"急速保留在机制,增伤暂定50%,其他动作和切换武器不影响。连续格挡仅刷新。自动反击吃"）
- D-P4 已决议:cd_reduce(急速)保留在**机制类**(数值型特例),理由=强化冷却操作逻辑+与attack_cd op绑定+B符文迅捷(Ehwaz)放大;§3.2#3已注明。
- D-P6 已决议(5项):①增伤X%=**暂定50%**(B符文守护Othala+15%→65%);②buff**不被其他动作/切换武器清除**,仅被下一击消耗(无超时);③**连续完美格挡仅刷新不叠加**;④B符文连锁(Nauthiz)自动反击**同样吃+50%**;⑤#6行更新为触发型buff+攻击结算×1.5。
- doc21同步:§3.2#6展开、§4 perfect_block_empower行(守护+50%再+15%→65%;连锁注记吃加成)、§7 D-P4/D-P6标已定、§9.1待决仅余D-P3。
- 当前被动系统doc21设计层争议**全部清完**,唯一余项为D-P3(代码实名制,需对照player.gd/移动/攻击/格挡控制器核实状态机名)——属落地前核实,非设计争议。


## D-P3 代码对齐核实（用户"执行"）
- 用户指令"执行"=落地D-P3:读 player.gd / state/* / project.godot [input],把 doc21 §3.4 预期映射替换为真实映射。
- 真实 input action: action=鼠标左键(攻击), block=鼠标右键(格挡/完美格挡, PlayerStateBlocking.BLOCK_WINDOW_SEC=0.3 与文档基线一致), jump=空格(仅地面 is_on_floor), run=Shift(奔跑), throw=R(82), kick/Charging=冲撞技能(F), skill_g=G/depart=T(技能槽), use=E(拾取)。
- 7 项缺口/脱节: (1) 轻弩装弹 reload_shot **代码未实现**(PlayerStateShooting 右键按住连射无装弹计时,与设计"不允许连射"相反); (2) 近战蓄力 charge **未实现**(仅远程 AttackPreparing 有蓄力); (3) 攻击 CD 系统**缺失**(cd_reduce 无字段可缩放); (4) 二段跳 air_dash **未实现**(Moving 要求 is_on_floor); (5) dodge **脱节**(dodge 是 ME.try_sidestep 被动侧垫步免伤非主动操作, doc21 #7 残影定义需重写); (6) **键冲突** throw 与 restart 同为 R(82); (7) 完美格挡 hook 点(BLOCK_WINDOW_SEC 硬编码需改被动可读 + empower 需挂 buff)。
- 完美格挡盾耐久(player.gd 504-508)确认: 持盾 0.3s 窗内不耗耐久、双手格挡不耗耐久,与设计基线一致。
- doc21 改动: §3.4 标题"已核实"+真实映射表+7项缺口清单; §3.2 #4 ram 改"冲撞技能F键"、#7 afterimage 改"受击侧垫步被动"、#11 注记 throw=R 且冲突; §7/§9.1 D-P3 标已核实。
- 结论: 被动系统**设计层全部完成**(D-P1~D-P9 决议); 剩余是**代码落地**(依缺口1/2/3实现机制类被动 hook)。

## 代码落地：近战蓄力基础 + 攻击CD系统 + 准心像素冷却环（用户新指令）
- 用户指令: 未装备蓄力技能时左键也可按住(骑砍式),但无增伤;攻击CD系统需添加(仅近战);准心两侧加UI——很小半个扇形对应左右手武器,单手/双手则完整环形,有冷却提示,像素风格。
- 落地产物(全部 GDScript,未跑 headless 因 godot-editor 未部署):
  - `player_state_data.gd`: 加 `weapon_charge_ratio` 字段 + `set_weapon_charge_ratio()`。
  - `skill_runtime.gd`: 加 `mechanism_passives` 字典 + `has_mechanism_passive`/`get_mechanism_passive_level`/`grant_mechanism_passive`(机制类被动=操作强化查询入口,doc21 §3)。
  - `player.gd`: 加近战CD字段(`_melee_cd_primary/secondary` 独立计时,双手×1.5/双持副手0.38s)、`_physics_process` 倒计时、`start_melee_cooldown(hand)`、`get_melee_cd_multiplier()`(cd_reduce×0.85)、`is_melee_on_cooldown(hand)`、`get_melee_cd_fill(hand)`、`has_mechanism_passive(id)`(路由 SkillRuntime)、`get_melee_charge_multiplier(ratio)`(无蓄力被动/蓄力0→1.0,装备后 lerp 1.0→2.0)。
  - `player_state_moving.gd`: 主/副手攻击输入加 CD 封锁(远程豁免)。
  - `player_state_attack_preparing.gd`: 近战新增"按住蓄力"分支(记录 ratio + 驱动 `ViewModel.apply_melee_charge` 前摇),取消蓄力时 restore_view_model。
  - `player_state_slashing.gd`: `_enter_tree` 触发 `start_melee_cooldown(hand)`;命中时按 `get_melee_charge_multiplier` 缩放 `final_damage`/`knockback_force`(敌人)与场景物伤害(整数)。
  - `view_model.gd`: 加 `apply_melee_charge(progress)`(武器后收下压前摇)。
  - `crosshair.gd`: 加像素冷却指示——`_update_cd_state` 拉取玩家模式(双持=2半扇/单双手=1环/远程=0隐藏),`_draw_pixel_arc` 用 RING_SEGS 像素块画,已恢复段亮绿 COL_CD_READY、冷却段暗灰 COL_CD_COOLING;就绪(满)时隐藏保持干净;签名量化 1/16 仅状态变化时重绘。custom_minimum_size 40→48。
  - `combat_hud.tscn`: Crosshair 节点 offset ±20→±24 适配 48 尺寸。
  - `docs/21`: §3.4 缺口2/3 标为已落地。
- 设计要点: 蓄力=基础操作(永远可按住),增伤仅在装备 `charge` 被动时生效(满足"无增伤效果"诉求);CD 仅近战,急速 cd_reduce 缩放之。

## 准心冷却UI修复：消除像素块重合（用户"注意准心UI不要重合"）
- 问题根因: `_draw_pixel_arc` 固定用 `RING_SEGS=18` 段,但双持半扇仅占 180° 弧 → 18 个 4px 块挤半圈,块间距 ~2.97px < 块宽 4px → **相邻像素块互相重叠糊成色块**。完整环 18 段铺 360°(间距~5.9px)无重叠;十字臂尖半径11、环内缘半径15,留4px不冲突。
- 修复: `_draw_pixel_arc` 增 `segs` 参数;双持半扇传 `int(RING_SEGS/2)=9`,块间距变 ~11.85px >> 4px,清晰分离;双扇边界(±90°)各留~6px空隙不交叉。全环仍用 18 段。
- 校验: 几何复核无任何重叠(块间/环与十字/双扇边界)。

## 代码落地：轻弩装弹 #1 + 二段跳 #4 + R键冲突 #6（用户"进行落地/继续"）
- **轻弩装弹 reload_shot（缺口#1，已落地）**: `player.gd` 加 `_crossbow_reload_remaining/_total` + `CROSSBOW_RELOAD_FALLBACK_SEC=1.2`；`start_crossbow_reload()`(读武器 `reload_time`，否则兜底1.2；`quick_reload`被动×0.8)、`is_crossbow_reloading()`、`get_crossbow_reload_fill()`；`_physics_process` 倒计时。`PlayerStateShooting._fire_projectile` 发射后调用 `start_crossbow_reload()`；`PlayerStateAiming` 装弹中封锁左键射击(不允许连续发射)。`weapon_data.gd` 加 `reload_time:float=0.0`（弩 .tres 可声明）。
- **二段跳 air_dash（缺口#4，已落地，受被动门控）**: `player.gd` 加 `_air_jumps_used`/`AIR_JUMPS_MAX=1`/`AIR_JUMP_FORCE_MULT=0.92` + `do_jump()`(地面跳基准；空中跳仅 `has_mechanism_passive("air_dash")` 且次数<上限时触发，力度×0.92)；落地 `_physics_process` 重置计数。`PlayerStateMoving` 跳跃输入改走 `player.do_jump()`，移除原 `is_on_floor()` 硬编码。因 air_dash 属机制类被动(DEX15里程碑/迅捷Ehwaz解锁)，未解锁仅能地面跳。
- **R键冲突（缺口#6，已修复）**: `project.godot` 的 `restart` 物理键码 82(R)→81(Q)，与 `throw`(R) 解耦；死亡界面按 Q 重启。`player_state_dying.gd` 仍读 "restart" 动作名不受影响。
- **准心琥珀装弹环**: `crosshair.gd` 加模式3——弩装弹时显示琥珀色完整环（与近战绿环同半径17互斥、不重叠），`_draw_pixel_arc` 增 `col_on/col_off` 可选色参；`_cd_signature` 增 `reload_%d`。修复控制流：原 `else` 分支会把模式3误判双持"满则 return"而吞掉装弹环，改为显式 `if/elif` 三模式。
- 文档 `docs/21` §3.4 缺口#1/#4/#6 标为已落地（#4 注明受被动门控）。
- 注意: godot-editor 未部署，无法跑 headless 编译/测试；改动均为标准 GDScript4 写法，已逐文件审查（含 _draw_cd 控制流修复、跨弓/弩/近战 CD 互斥不重叠、air jump 计数边界）。

## 缺口#5 dodge重定义 + #7 完美格挡hook（第三批落地）
- **缺口#5 dodge脱节·已重定义(docs/21 #7 afterimage)**: 原 `dodge` 在代码是里程碑被动 `ME.try_sidestep` 的随机免伤(无主动操作)，与设计"残影"脱节。重定义为**侧垫步触发后的增伤**: `player.gd` try_receive_hit_result 中 `ME.try_sidestep` 免伤成功且 `has_mechanism_passive("afterimage")` 时调 `set_sidestep_buff()`(1.5s窗口)；`player_state_slashing._resolve_enemy_hit` 命中时 `consume_sidestep_buff()` 叠 ×1.3。侧垫步(AGI T1里程碑)与 afterimage(机制被动)分属不同层、互不替代。
- **缺口#7 完美格挡hook·已落地**:
  - *窗口延长 perfect_block_window*: `player_state_blocking.gd` 保留 `const BLOCK_WINDOW_SEC:=0.3`(满足 action_block_test 字符串断言)，新增 `get_perfect_block_window_sec()`——装备被动返回0.8否则0.3；`_process`双手超时检查与 `is_in_grace_window()` 均改用它。**同时作用于持盾完美格挡与双手武器精确格挡**(D-P5已决议)。
  - *增伤 perfect_block_empower*: `player.gd` try_receive_hit_result 的 BLOCKING 分支在 grace 窗口内被攻击且 `has_mechanism_passive("perfect_block_empower")` 时 `set_perfect_block_buff()`；slashing 命中 `consume_perfect_block_buff()` 叠 ×1.5。buff 切武器不清除、连续完美格挡仅刷新不叠加、B符文连锁自动反击走同一结算入口故同样享受+50%。
  - `player.gd` 新增字段 `_perfect_block_buff_active`/`_sidestep_buff_remaining` + 常量 `PERFECT_BLOCK_BUFF_MULT=1.5`/`SIDESTEP_BUFF_MULT=1.3`/`SIDESTEP_BUFF_SEC=1.5` + 4 个方法(set/consume ×2)；`_physics_process` 加 `_sidestep_buff_remaining` 倒计时。
  - slashing 倍率组合 `dmg_mult = _charge_multiplier() × (1.5 if pb) × (1.3 if si)`，一次挥砍仅消费一次(多敌仅首敌享受)，miss 不消耗(留待下次命中)。
- 文档 `docs/21` §3.4 缺口#5/#7 标为已落地。
- 待办: perfect_block_window/empower/afterimage 三个机制被动尚未接入解锁阶梯(需在 SkillRuntime 用 grant_mechanism_passive 授予后才生效；未授予时功能自然关闭=无效果，语义正确)。

## 机制被动接入解锁阶梯(用户"接入")
- 实现 `SkillRuntime.recompute_mechanism_passives()`：幂等全量重算授予(先 `mechanism_passives.clear()` 再按阶梯条件 `grant_mechanism_passive`)。
- 授予条件(对齐 doc21 §5/§7):
  - 属性 T2(=15点, MILESTONE_THRESHOLD[T2]) → `air_dash`(DEX)/`cd_reduce`(PER)/`afterimage`(AGI)。
  - 武器 tier(`SD.can_unlock(tier,prof,attr)`, T2=1/T3=2): `charge`=全近战T2(one_hand_melee或two_hand任一线); `perfect_block_window`=双手T2或持盾流派(one_hand_melee)T2; `perfect_block_empower`=双手T3或持盾T2; `quick_reload`=轻弩T2。
  - 注意: 枚举名(SD.AttrMilestone.T2)在本项目不可靠(无 class_name), 统一用 int 字面量 1/2 + 注释; 熟练度键用 medium 串("one_hand_melee"/"two_hand"/"crossbow")。
- 触发点: `AttrPanel.accumulate_attr`/`accumulate_proficiency`/`check_skill_unlocks`/`deserialize` + `player._ready`(经 `Service.skill_runtime()`)。跨 autoload 调用统一 `Engine.get_main_loop().root.get_node_or_null("SkillRuntime")`(本项目不靠全局名)。
- `ram`/`throw_recover` 代码侧无 hook, 暂不授予(待实现后纳入本函数)。
- `SkillRuntime.reset()` 加 `mechanism_passives.clear()`; 无需 serialize(派生量, 读档靠 deserialize 调 recompute)。
- 注意: recompute 每次 clear 后重授, 若未来符文也要 grant 机制被动需改此函数(目前无符文→机制被动路径)。

## 文档↔代码对应审计(doc21 ↔ 代码, 用户"检查对应情况")
- **已对齐(文档真实反映代码)**:
  - §3.4 缺口#1–#7 全部落地(轻弩装弹/近战蓄力/攻击CD/二段跳/dodge重定义/R键冲突/完美格挡hook)。已 grep 核实所有相关常量/方法/调用点存在; project.godot 确认 restart 82→81(Q)、throw 仍 82(R); blocking.gd `get_perfect_block_window_sec()` 装备时返 0.8 否则 0.3(非仅常数)。
  - recompute 授予的 7 个机制被动 与 代码 7 个 `has_mechanism_passive` 调用点一一对应。
  - 准心装弹环 mode 3 (`_cd_mode==3`/`COL_RELOAD_*`) 已实现。
- **文档不准确/缺口(待修正)**:
  - **A. 未实现机制被动: 文档说 2 个, 实际 4 个**。doc21 L207 写"ram 与 throw_recover 未建 hook"; 但 §5 表列 11 个机制被动中 `ram`/`cancel_recovery`/`auto_loot`/`throw_recover` 共 4 个既无代码 hook 也未被 recompute 授予(漏提 cancel_recovery 取消后摇、auto_loot 自动拾取)。
  - **B. 数值类被动(§2, 13 项)整体为设计态, 代码无对应**。被动名(利刃精通/钝击强化/坚壁/巨盾支撑…)全文仅出现在 doc21, 无任何 .gd 定义; skill_data.gd 真实 passive 段是 `ATTR_MILESTONES`(18 项, effect_type: kb_chance/melee_dmg_up/ranged_hit_up/dodge_chance…), 与 §2 完全不重合; 目标 stat 键(damage_mult/block_value/reload_time)虽为武器字段但无代码把 §2 那 13 个被动写入。§2.3"落面板聚合点"属设计意图未落地。
  - **C. doc21 L294 过度宣称**: "目前仅 cd_reduce 与 巨盾支撑(格挡耐力−30%)[已实现]"——`block_stamina` 代码无 trace, 巨盾支撑未实现。
  - **D. 三套被动体系并存未桥接**: ① 代码 ATTR_MILESTONES(skill_data.gd, 18项, MilestoneEffects 应用); ② doc21 §3 机制被动(11项, 7项已桥接 via mechanism_passives, 4项未); ③ doc21 §2 数值被动(13项, 纯设计)。三者语义有重叠(如 ram↔震退 都涉击退)但无代码桥接。
- 结论: §3.4 七缺口与 §3 的 7 项机制被动真实落地; 但 §2 数值被动+ §3 的 4 项机制被动仍是设计态, 且 doc21 对自身未落地部分标注不全(尤 L207/L294)。建议修正 L207/L294 并补"三体系并存"说明段。

## 移除耐力系统(用户"不限制耐力, 把耐力有关的全部替换掉")
- 决策: 本作**不引入耐力资源**, 所有动作仅受冷却/前摇限制。耐力在代码里本就无真实资源/消耗逻辑, 仅 UI 展示文本 + 设计文档引用。
- 代码改动:
  - `scenes/ui/character_panel.gd`: 技能库 3 个技能(重击/迅捷回旋/坚盾壁垒) `cost` 由 "15/10/20 Stamina" → "None"(与已有 Adrenaline Rush 一致, 仅受冷却限制); 饰品戒指描述 "Increases max stamina slightly" → "Increases max health slightly"(tr key 需与 csv 一致)。
  - `scenes/ui/localization/translations.csv`: 删除 3 条失效的耐力 cost 翻译行(15/10/20 耐力); 戒指描述行改生命上限("略微提高最大耐力"→"略微提高生命上限")。
  - 注: "None" 不在 csv, 但 Godot tr() 无翻译回退键名(Adrenaline Rush 已用, 行为稳定)。
- 设计文档改动:
  - `docs/21`: §2 stat 联合类型删 `block_stamina`; 删巨盾支撑行(原 +0.30 格挡耐力消耗−30%, 即审计 C 所指未实现项); stat 表删 `block_stamina` 行; §3 机制表删 "可消耗耐力"/"消耗耐力"; §3 JSON 示例删 params.stamina; §9 轴3 改写为"不引入耐力资源"并去掉 RESOURCE:stamina。
  - `docs/99`: 待办"耐力/负重速度惩罚"→"负重速度惩罚"。
  - 中文"体力/体力活"(docs/07/01) 指体力劳作非游戏耐力, 属误报, 不动。
- 审计修正: 原审计 C"巨盾支撑未实现"已随本次删除解决(不再是未实现项, 直接移除该被动设计)。doc21 现仅剩 §2 数值被动+ §3 的 4 项机制被动(cancel_recovery/auto_loot/ram/throw_recover)为设计态——其中 §2 的"坚壁"等仍保留(非耐力相关)。

## 战斗模型去回合制残留(命中率/伤害骰/护甲闪避) — 全链路剥离
- 用户指令(承接): "全部都需要重做,甚至还有命中率的回合制残留" + AskUserQuestion 决议: q-0 连伤害骰一起重做(剥离整个数值模型的回合制残留: 命中率+hit_bonus+伤害骰NdN+暴击/防御RPG残留); q-1 先移除,后续设计构建(精度/命中维度); q-2 保留proc(侧垫步/魔力凝息/招架反击等ARPG常态随机触发,不动)。
- **hit_bonus 系统全链路移除**:
  - `globals/combat/skill_data.gd` SKILLS: 删 30 处 `"hit_bonus": <n>,`(python regex 剥离, 顺带折叠双逗号); action_skills.gd 删 5 处。头部注释标注该字段已移除。
  - `data/weapon_data.gd`: 删 `@export var hit_bonus_percent`/`shield_block_chance_percent`/`armor_evade_percent`(后两者属防御/闪避残留)。
  - `globals/combat/damage_resolver.gd`: `_compute_base_damage` 由 randi_range 真实投骰 → 确定性均值(`dice_count×(sides+1)/2 + flat`); 删 STYLE_META 的 `hit_bonus` 键(ONE_HAND 10/DUAL_WIELD -10)。
  - `globals/combat/combat_bridge.gd`: 删 `weapon_hit_bonus` 写入; `_apply_milestones_to_attack` 用 `ME.apply_sharpshooter_crit`(远程暴击+10)/`ME.apply_penetrating_damage`(远程伤害×1.12) 取代旧的 `apply_sharpshooter`/`apply_penetrating_strike`(已删)。
  - `globals/equipment/affix_system.gd`: focused/worn `hit_bonus_add`→`crit_bonus_add`(命中率→暴击率); 删 `evade_bonus_add`。
  - UI: `equipment_detail_popup.gd` 删 Hit/Evade 显示块; `equipment_panel_combat_stats.gd` 删 "Hit Mod" 行, "Damage Dice %s" 改为 "Damage ~%.1f"(显示确定性均值, 去骰RPG化)。
  - `translations.csv`: 同步改键 Damage Dice→Damage ~, Hit Mod→Crit, 及 5 个技能desc(重力击/神射手/穿透打击/直觉闪避/魔力涌动)匹配 skill_data.gd 新 desc。
  - `docs/21`: 神射手/穿透打击 行更新为新效果+新函数名。
  - 测试全修: combat_engine_test(删 hit_bonus 断言, 新增 STYLE_META 无 hit_bonus 负向校验)/combat_bridge_test(删 weapon_hit_bonus, 远程里程碑改断言 crit_bonus==10 & weapon_damage_mult==1.12)/milestone_effects_test(改测 apply_sharpshooter_crit/apply_penetrating_damage)/equipment_generation_test(删 hit_bonus_percent, focused→crit_bonus)/affix_ui_display_test(删 hit_bonus_percent, 改测 Crit 行)/skill_data_test(删 hit_bonus 断言)/tavern_equipment_panel_test(命中率→暴击率 行校验)。
- **armor_evade_percent 连带修复(发现即修)**: 上一轮已删 `armor_evade_percent` 字段但漏删消费者, 致 `equipment_component.gd:get_armor_evade_bonus()` 引用不存在属性(编译期即崩)。本次补全移除: 删该函数 + 全测试引用(equipment_component_test/weapon_registry_test/bugfix_equipment_panel_test/combat_bridge_test/expedition_failure_test)。该字段为"防御RPG残留", UI 闪避率(compute_evade_rate, agi×1%+虚实避让+6%)本就不读它 → 删除无玩法影响, 符合 q-0。
- **静态校验**: 全仓 grep 确认 `weapon_hit_bonus|apply_sharpshooter\b|apply_penetrating_strike|hit_bonus_percent|get_armor_evade_bonus|armor_evade_percent|Damage Dice|Hit Mod|命中修正|\.hit_bonus|"hit_bonus"` 仅剩 2 处**有意**引用(combat_engine_test 负向断言 + tavern_equipment_panel_test 注释)。longbow 无 damage_mult 覆写→默认1.0, ×1.12=1.12 与测试断言一致。
- **未决(用户q-1"设计later")**: 18项属性里程碑被动整体重做(Task#16)、doc21 全量重写、其余命名词缀desc里仍含"命中"/"投骰"的旧技能(如贯穿刺击/蓄力等)待统一动作化措辞。Godot 二进制本环境缺失, 未跑 headless 测试, 需在编辑器/CI 验证。
