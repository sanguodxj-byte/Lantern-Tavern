# 2026-07-26

## 深度架构审查与优化

- 完成全项目架构审查（globals 30 个 autoload / scenes 层 / 工程配置）。
- 修复存档遗漏（最高危问题）：SaveManager 新增 TavernSettlement（声望/常客名册）、ArmorProficiency（护甲熟练度）、ZoneManager（选定区域）的序列化，tavern_settlement.gd 新增 serialize/deserialize/reset_state 与 Customer 字典转换（weapon_data 资源不入档，读档时经 WeaponRegistry 重建）。
- 统一单例访问：service.gd 新增 armor_proficiency()；tavern_settlement/loot_table/skill_runtime 的 ad-hoc get_node_or_null 改走 Service。
- tutorial_tavern_coordinator.gd 改为优先经 WeaponRegistry 取 axe 数据（保留 .tres fallback）。
- save_manager_test.gd 新增 3 个 roundtrip 测试；24/24 通过，invalid_reference_scan 7/7 通过。
- 测试运行方式：沙箱会拦截 Godot 写 user:// 目录，须走 dangerouslyDisableSandbox + 项目 _run_test.bat 模式（bat 内绝对路径 D:\123\Godot_v4.7-stable_mono_win64.exe）。
- 未处理的已知问题（供后续）：tavern_equipment_panel.gd 1791 行 / player.gd 1258 行需拆分；model_viewer.gd:99 与 view_model.gd/first_person_arm_animator.gd 武器硬编码字典应下沉 weapons.json；player/enemy 受击流程重复；main_scene 指向 tools/ui_runtime_capture_world.tscn；reports/ 2469 文件未被 git 忽略；_settle_human p_base=30 为占位值。
- 方案2 已落地游戏内：新增 `scenes/world/pixel_view.gd`（PixelView, SubViewportContainer, stretch_shrink=4 + NEAREST + 关 MSAA/TAA/SSAA），挂进 `scenes/world/world.tscn`（UI 节点之前）。SubViewport 共享主 World3D（own_world_3d=false），每帧同步主视口相机全部参数，激活后 `get_viewport().disable_3d=true` 避免双重渲染；UI CanvasLayer 保持全分辨率。
- 验证：`tools/pixel_view_ingame_capture.gd`（非 headless + opengl3 + --log-file）截图 `reports/retro_pixel_preview/ingame_pixel_view.png`；invalid_reference_scan_test 7/7 通过。

## God script 拆分（任务 #5/#6 完成）

- tavern_equipment_panel.gd 1791→1677 行：提取 `scenes/ui/equipment_slot_hint_icons.gd`（槽位提示轮廓 flood-fill）与 `scenes/ui/equipment_panel_inventory.gd`（背包/仓库存取转移），panel 内为一行委托；同步更新 equipment_slot_display_test / bugfix_equipment_panel_test 的源码契约断言；面板相关 5 套件 75/75 通过（顺带修复 stat_indicator.tscn 缺 theme/texture_filter 的预存失败）。
- player.gd 1258→1152 行：提取 `scenes/characters/player/player_combat_runtime.gd`（PlayerCombatRuntime, RefCounted，同 CombatBuffComponent 模式）。承载：近战冷却（主/副手）、轻弩装弹、二段跳计数、完美格挡/残影 buff、流派专精被动运行时（蓄势/暴风骤雨/元素环/交错挥砍/奥术护盾）及全部相关常量。player.gd 保留同名公共 API 薄委托；`_melee_cd_primary/_secondary` 以属性 getter/setter 兼容测试直接读写；常量 MELEE_CHARGE_FULL_SEC 等以 `:= PLAYER_COMBAT_RUNTIME.X` 转发（player_state_attack_preparing/slashing 引用 player.MELEE_CHARGE_FULL_SEC 等不受影响）。
- 修复测试随机性：style_passive_effects_test.gd 的 test_damage_resolver_no_style_passive_baseline 有 5% 概率随机暴击 ×1.5 flake（damage_resolver 基础暴击率 5%），加 `attack.crit_bonus = -100.0` 压制。
- 玩家相关 13 套件 204/204 通过。临时 bat（_run_player_tests.bat/_run_panel_tests.bat）与结果 txt 已清理。

## 工程卫生收尾

- project.godot：main_scene 从 `tools/ui_runtime_capture_world.tscn`（截图工具）改回 `scenes/ui/main_menu.tscn`（project_bootstrap_test 期望值）；顺带删除残留 `[dotnet]` 段（纯 GDScript 项目，该段会使 Mono 编辑器拒绝 Web 导出，测试本就断言其不存在）。project_bootstrap + invalid_reference_scan 12/12 通过。
- reports/ 已确认在 .gitignore 中且 git 未跟踪任何文件，无需处理。
- 仍保留的已知小问题：restart 与 throw 均绑定物理 R 键（实际无冲突：restart 仅 dying 状态读取、throw 仅 moving 状态读取）；GameEvents 被绕过。

## 剩余修复（任务 #7/#8 完成）

- #8 `_settle_human` p_base 占位修复：tavern_settlement.gd 新增 `_lookup_recipe_base_price(brew_flavors)` + `_flavors_match`，按口味字典与 BrewingData.RECIPES_DB 各酒谱 expected_flavors 完全匹配反查人类标价（price=null 的怪物酒谱跳过），未匹配回落 `HUMAN_DEFAULT_BASE_PRICE=30`。
- #7 武器视觉参数下沉 weapons.json：12 个武器/盾条目新增 `first_person: {view_scale, mount_position, mount_rotation}`；weapon_registry.gd 新增 `_first_person` 存储 + `get_first_person_params(id)`；view_model.gd 的 `_weapon_view_scale()` 与 `_apply_weapon_mount_pose()` 首选注册表作者化参数、未配置回落 profile 默认（hardcode match 保留为 fallback，兼容旧 .tres 无 id 武器）。
- model_viewer.gd：装备中文名不再硬编码，`_filename_to_display_name` 新增 `_resolve_equipment_display_name`（按 id 查 WeaponRegistry.get_display_name，weapons.json name_zh 为唯一数据源）；_MODEL_NAME_ZH 仅保留注册表外遗留模型（longsword/buckler）。注意注册表名与旧硬编码不同：axe=双手斧、chain_armor=链甲、spear=长枪、crossbow=轻弩、greatsword=双手大剑，model_viewer_test 断言已同步。
- 顺带修复 2 个预存坏测试 + 1 个数据缺口：weapon_registry_test 轻/重甲数量 2→5（头盔/护腕/靴子早已加入）；`get_weapon()` 不存在改为 `get_weapon_data()`；weapons.json sword 条目补上缺失的 `view_model_profile: "sword"`（测试早已断言）。
- 验证：weapon_registry/model_viewer/view_model/customer_system/crossbow_behavior/equipment_generation/player_animation_waterfall/viewer_panel_integration 8 套件 223/223 通过；brewing_data/save_manager/full_flow_integration 76/76 通过。
- first_person_arm_animator.gd 的骨骼姿态字典（每 profile 的 UpperArm/LowerArm 欧拉角）属于动画作者内容而非装备数据，保留在代码中（未下沉）。

## 全身板甲主角模型

- 评估五个候选主角模型 A-E，选定 **Scheme A (Lantern Keeper)** 作为基础：比例最协调、部件语义最丰富、前/侧深度最好、整体质量最高。
- 新增单模型生成器 `tools/generate_voxel_player_plate.py`（model_id=player_plate），基于 Scheme A 的已验证无重叠几何体，全身替换为钢制板甲并添加外部装甲部件：
  - 头部：全罩大头盔 + 面甲横条 + 护鼻 + 眼缝露眼 + 红色羽冠；
  - 躯干：蓝钢胸甲（ breastplate/背板）、护喉、黄铜腰带与中央饰线、腰甲；
  - 手臂：肩甲（pauldron）、上臂钢筒、前臂铁铠、金属手套；
  - 腿部：大腿甲、膝甲、胫甲、带铁鞋头的铁靴；
  - 全身无皮肤/头发裸露，实现“全身覆盖”。
- 严格遵循 `1m=32px` 与面接触装配规则；生成器内调用 `assert_parts_no_positive_volume_overlap` + `assert_parts_single_face_connected_component`，经 3 轮迭代修正 tasset/fauld/poleyn/gauntlet/thumb/visor 等零件的穿透问题后通过。
- 输出：`assets/meshes/characters/voxel_player_plate.glb`；三视图+真 3D 渲染：`reports/characters_preview/voxel_player_plate_render_{preview,front,side,top}.png`。
- Godot 导入后用 `voxel_model_test_support.gd` 复测：0 处正体积重叠、0 处面断开，AABB (24.0, 59.0, 17.5)px 与 Blender 生成器一致。
- 制作 side-by-side 对比图：`reports/characters_preview/player_scheme_a_to_plate_comparison.png`。

- 用户进一步澄清：盔甲应“独立建模”，是叠加在角色身上的空心外壳，不能与身体有任何重合。据此重写 `tools/generate_voxel_player_plate.py`：
  - 以 59 块独立钢板的空心笼（hollow shell）包裹虚拟身体，各板 `CLEARANCE_PX=3.0` 位于身体表面外侧，盔甲与身体几何体完全分离。
  - 修正肩部 `+z` 顶板剩余 2 处穿透：将 `sho_l/sho_r` 顶板 x 方向缩窄 4px，使其内缘停在身体 x=±10 边界（面接触），彻底消除与颈/肩部的正体积重叠。
  - 验证结果：`armour self-overlap = 0`，`armour-vs-body positive-volume overlap = 0`；盔甲包围盒 (W37.0, D28.0, H66.5 px) 大于 Scheme A 身体包围盒 (W24.0, D19.5, H59.0 px)，满足“大一圈”。
  - 输出：`assets/meshes/characters/voxel_player_plate.glb`；独立渲染与穿戴预览：`reports/characters_preview/voxel_player_plate_{render,worn}_*.png`。
  - 将生成器与 `tools/_check_armor_fit.py` 的输出路径改为绝对路径（锚定项目根），避免 Blender 渲染输出落到 `C:\reports`。
  - 清理临时脚本 `tools/_measure_aabb.py`、`tools/_dbg_body.py` 与 `C:\reports` 残留目录；保留 `tools/_check_armor_fit.py` 作为后续板甲合身检查工具。

