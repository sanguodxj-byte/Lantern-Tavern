# 2026-07-12 工作日志

## 校正既有测试失败（非联机改动，属独立 bug 修复）
用户要求"先校正测试失败，检查是否预期内"。发现两类：

### 1. combat_bridge_test.gd（2 失败）— 预期内的陈旧引用，已校正
- 根因：测试 line 142、438 仍对 `WeaponData.shield_block_chance_percent` 赋值，但该字段已在更早会话按"动作控制战斗模型剥离回合制残留"决议移除（`WeaponData` 现仅 `shield_block_value: int`）。测试自身注释也写明"已移除"。
- 修复：删除两条陈旧赋值。断言只查 `armor_def` 与 `defender` 是否含 `shield_block_chance`，不需要该字段。
- 验证：`combat_bridge_test` 37 用例 | 0 errors | 0 failures。
- 结论：**预期内**（已知字段移除的测试债务），已修正。

### 2. procedural_dungeon.gd（真实编译错误，非预期内）— 已修复 2 处
- 错误 A（line 561）：`if player_spawned:` 块主体全为注释（旧 spawn 调用被注释后无有效语句）→ GDScript "Expected indented block after if block"。修复：注释后加 `pass`（零行为变化，作者本意保留空分支待接 downstairs portal）。
- 错误 B（line 1517）：`var pending_batches := (三元表达式)` 类型推断失败（batched_decor_transforms 与 Dictionary() 无法统一推断）→ "Cannot infer the type"。修复：显式标注 `: Dictionary`。
- 这两处是同一文件的多层既有编译错误（A 挡住编译后才暴露 B），与联机工作无关，属地牢重构期引入的真 bug。
- 验证：临时单测 `load("res://scenes/expedition/procedural_dungeon.gd")` 成功（1/0/0）；原 `procedural_dungeon_test.gd::test_procedural_dungeon_script_compiles` 等价逻辑通过（runner 不支持 `文件::用例` 语法，需用临时测试或整文件跑）。
- 结论：**非预期内**（脚本无法编译=地牢场景跑不起来），已修复。

### 环境/验证备注
- gdUnit4 runner `-a` **不支持** `文件::用例名` 语法（报 "Given directory or file does not exists" + "No test cases found" 空跑，看似无错实为没跑）。精确定位单用例需临时测试或整文件跑。
- `--check-only --script res://...` 隔离编译会报 `Identifier not found: WeaponRegistry/PhysicsSetup/...`（autoload/全局类未在隔离模式注册），属假阳性；真实项目加载下这些标识符解析正常（combat_bridge_test 能跑 37 用例即证）。
- 测试 recipe 同前：`export APPDATA="D:/123/Lantern Tavern/.tmp_apdata"` + 授权沙箱 `dangerouslyDisableSandbox`，Godot 二进制在 D:/123 需提权。

## 第四轮：SessionRoot 实现 + 联机测试部署（2026-07-12 晚）
用户要求"继续推进并部署测试"。

### 落地的代码
- `globals/multiplayer/session_root.gd`：由占位 `_ready: pass` 升级为**真实权威编排器**（extends Node，不声明 class_name）：
  - `_init()` 构建 PlayerRegistry/WorldState/CommandRouter/CommandValidator + 三权威系统实例；`_live_state`(peer→{peer_id,is_alive,position})、`_entities`、`_ctx_peer`(ctx 实例id→peer_id 反向索引)、`_seq_tracker` 均在 `_init` 内初始化。
  - `init_server()`/`init_client()`；`register_player`/`handle_spawn_request`(为 peer 建独立 PlayerContext: AttrPanel.init_defaults + SkillRuntime.init_defaults + ExpeditionInventory + EquipmentLoadout → `PlayerContext.for_peer(attrs,sk,inv,lo,null)`)/`unregister_player`(断线清理 + seq_tracker.reset)。
  - `on_command(peer_id, command)`：校验 protocol/world_revision → live.is_alive → ctx 存在 → `router.route`。
  - `wire_default_authorities()`：CMD_INTERACT/CMD_PICKUP→`_handle_interaction`(调用 InteractionAuthority.resolve_interaction，用服务器权威玩家位置)；CMD_ATTACK→`_handle_combat`(用 `CommandValidator.SequenceTracker` 校验序列 + 从服务器 `PlayerContext` 属性构造 `DamageResolver.AttackInput` + 调用 `CombatAuthority.resolve_attack`，产出 `combat_resolved` 事件)。
  - `server_roll_loot(table,seed)`(包 LootAuthority，敌人死亡掉落用)；`set_entity/remove_entity/get_entity`；`set_player_position/set_player_alive`；`build_session_snapshot/apply_session_snapshot`(重连)。
- `tests/gdunit/session_root_test.gd`：14 用例（子对象构建、两玩家独立上下文、命令权限边界、注入 handler 路由、交互拾取端到端+重复消费拒、超距拒、战斗结算+序列重放拒、断线清理、快照回放、掉落确定性）。
- `tools/run_multiplayer_tests.sh`：联机测试**部署脚本**（13 个联机测试 + attr_panel_test + combat_bridge_test 回归守卫；APPDATA 工作区内、每测试独立进程+日志、stdout 写文件后 grep、绝不管道接 head/tail）。后台运行验证中。

### 关键口径（再次确认，避免回归）
- `PlayerContext.for_peer(attrs, sk, inv, lo, player)` —— **无 peer_id / is_alive 字段**（docs/25 §4.1 提议未落地）；可信玩家状态走服务器 `_live_state` 字典。
- 子权威模块签名：`InteractionAuthority.resolve_interaction(command, ctx, live, entities)`；`CombatAuthority.validate_attack_request(req, live, server_rev, seq_tracker)` + `resolve_attack(ai, defender_data, forward, attacker_peer, defender_entity_id)`；`LootAuthority.roll_loot(table, rng, max_items)`；`DamageResolver.AttackInput`/`Defender` 字段见 damage_resolver.gd（attacker_str/dex/mag/per/agi/con/attack_type/style/weapon_damage_dice 等）。
- 内层类访问：`CommandValidator.SequenceTracker` / `DamageResolver.AttackInput` 经 const 预载脚本别名可访问（与 combat_authority.gd 一致）。

### 仍未做（需场景/ENet 联调，风险更高）
- Phase 4 输入帧(`input_frame` RPC)/位置快照、真实 `MultiplayerSpawner` 玩家节点生成、客户端插值/纠正——这些要碰实际 Player/World 场景与双进程 ENet，建议在编辑器/CI 验证。

## 第五轮：Phase 4 移动权威逻辑层（2026-07-12 凌晨，部署验证中）
用户说"继续"。在第四轮 SessionRoot 基础上推进 Phase 4 纯逻辑层：

### 新增 `globals/multiplayer/movement_authority.gd`（extends RefCounted，无场景树依赖）
- `validate_input_frame(command, live, server_rev, seq_tracker)`：校验协议/修订/存活/peer>0 → move 必须为两分量数值、分量∈[-1,1]、模长≤1（防速度作弊）→ **最后**才 `seq_tracker.accept`（静态校验失败不消耗序列，避免合法帧被判重放）。
- `integrate_position(old_pos, move_vec, dt, speed)`：服务器从输入积分权威位置（y 轴恒 0），`dir=Vector3(move.x,0,move.y)`；**绝不信任客户端自报坐标**（防穿墙/瞬移/速度作弊）。
- `resolve_input_frame(...)`：校验→积分→产出 `player_snapshot` 事件（含 peer_id/position/look_yaw/look_pitch/sequence）。
- 常量：`BASE_SPEED=4.0` / `SPRINT_MULT=1.6` / `DEFAULT_TICK_DT=1/30`；sprint 提速经 speed 倍率。
- 配套 `movement_authority.gd.uid`（新建）。

### SessionRoot 接线
- 新增 `MovementAuthorityClass` preload、`movement_auth` 实例（`_init` 内 new）、`SERVER_TICK_DT=1/30` 常量。
- `wire_default_authorities()` 加 `register_authority(NP.CMD_INPUT, _handle_movement)`；`_handle_movement` 经 `movement_auth.resolve_input_frame` 结算并 `set_player_position` 写回 `_live_state`。
- `on_command` 全链路：客户端 `input_frame` → 权限校验 → router → `_handle_movement` → 权威位置积分 → `player_snapshot` 下发。

### 测试
- 新增 `tests/gdunit/movement_authority_test.gd`：11 用例（协议/修订/存活/非数组move/未归一化/越界分量/序列重放/非法帧不消耗序列/积分方向/冲刺提速/快照事件/死亡拒）。
- `session_root_test.gd` 加 2 用例：输入帧位置积分端到端 + 冲刺对比；`test_init_server_wires_authorities` 增加 CMD_INPUT 断言（现 17 用例）。
- `tools/run_multiplayer_tests.sh` TESTS 数组加 `movement_authority_test.gd`。
- **部署验证在后台跑（Q0wHyF），预期 16/16 全绿**（含既有 session_root_test 15→17 用例、movement_authority_test 11 用例）。

### 口径
- 序列号：input_frame 与 attack 共用 per-peer `SequenceTracker`（同一严格递增序列防重放），消费顺序经设计保证（静态校验先、序列消费后）。
- Phase 4 逻辑层闭环后，垂直切片覆盖：双玩家生成→输入帧驱动移动→拾取→攻击→确定性掉落→断线清理→重连快照。**剩余仍 ⬜**：真实 RPC 接线、MultiplayerSpawner 节点生成、客户端插值、双进程 ENet 联调（需编辑器/CI）。

## 第六轮：Phase 9 实体同步权威逻辑层（2026-07-12 凌晨，部署验证完成）
用户说"继续"→后要求"检查测试部署结果"。

### 新增 `globals/multiplayer/entity_sync_authority.gd`（extends RefCounted）
- `spawn_entity(entity_id, data, is_server)`：重复生成→失败；否则写 _entities，产出 `entity_spawned` 事件（含 entity_id/type/position/state）。
- `despawn_entity(entity_id)`：不存在→失败；否则 erase，产出 `entity_despawned` 事件。
- `update_entity(entity_id, patch)`：不存在→失败；否则 merge patch，产出 `entity_snapshot` 事件（含最新完整状态）。
- `build_delta(prev: Dictionary, curr: Dictionary)`：`prev`=客户端已知（entity_id→已知状态），`curr`=服务器当前（entity_id→状态）。新增→`entity_spawned`；变化（type/position/state 任一不同）→`entity_snapshot`；消失→`entity_despawned`。返回事件数组——增量同步核心。
- 配套 `.uid` 新建。

### SessionRoot 接线（零行为变化）
- 新增 `EntitySyncAuthorityClass` preload + `entity_auth` 实例（`_init` 内 new）。
- 原 `set_entity/remove_entity/get_entity` 重写改走 `entity_auth`；新增 `update_entity(entity_id, patch)`；`on_command` 流程不变。旧调用点（`set_entity`/`remove_entity`/`get_entity`）签名兼容。

### 测试
- 新增 `tests/gdunit/entity_sync_authority_test.gd`：11 用例（spawn/重复spawn失败/despawn/不存在despawn失败/update/不存在update失败/快照事件/全量spawn→build_delta→全量despawn 增量/消失delta/变化delta）。
- `session_root_test.gd` 加 1 集成用例（set_entity 触发 entity_spawned 事件、get_entity 返回值），现 18 用例。
- `tools/run_multiplayer_tests.sh` TESTS 数组加 `entity_sync_authority_test.gd`。

### 部署结果（emoxi4，2026-07-12 01:44 完成）
- **DEPLOY RESULT: PASS=17 FAIL=0**。17 套件 / 151 用例全绿。
- 含新套件：movement_authority_test 12/0/0、entity_sync_authority_test 11/0/0、session_root_test 18/0/0；回归守卫 combat_bridge_test 37/0/0、attr_panel_test 17/0/0 仍全绿。
- 注意：Orphan 计数（command_router 10 / interaction 8 / session_root 68 等）是 gdUnit 把目录内其它测试文件当 orphan，**不参与失败判定**，仅以 `Overall Summary` 的 errors/failures 为准。
- **关键修复（第五轮重跑 PQwdaH 已落实 + 本轮回填确认）**：gdUnit4 本版本 `assert_float(x).is_equal_approx(value)` 需两参 `(expected, tolerance)`，单参会编译失败→无汇总行；部署脚本已加固：**无汇总行即判 FAIL**，杜绝"解析崩溃被误判 PASS"假绿。

### 累计已落地（纯逻辑 / 可 headless 单测，全部实跑验证）
Phase 路线内模块：PlayerRegistry、WorldState、CommandValidator(+SequenceTracker)、NetworkProtocol、CommandRouter、SnapshotReplicator、InteractionAuthority、LootAuthority、CombatAuthority、SessionRoot(编排器)、MovementAuthority、EntitySyncAuthority。
剩余 ⬜：Phase 7 地牢 seed/layout 同步、酒馆共享操作；Phase 8 断线/重连真实 RPC + 双进程 ENet 集成测试（需编辑器/CI）；真实场景 MultiplayerSpawner 节点生成 + 客户端插值 + 快照下发 RPC。

## 第七轮：Phase 7 地牢 seed/layout 同步（2026-07-12 凌晨，部署验证 PASS=18/0）
（详见上方 2026-07-12 02:0x 段落；结果 DEPLOY RESULT PASS=18 FAIL=0，166 用例全绿。）
- 新增 `globals/multiplayer/dungeon_authority.gd`（extends RefCounted）：`start_expedition`/`end_expedition`/`make_layout_event`/`validate_layout_request`/`derive_layout_id`(确定性指纹)/`serialize`/`deserialize`。
- NP 新增 `CMD_REQUEST_LAYOUT`/`EVT_DUNGEON_LAYOUT`/`ERR_DUNGEON_LAYOUT_VERSION`/`ERR_DUNGEON_SEED_MISMATCH` 并同步 is_valid_command/is_valid_event。
- SessionRoot 加 `dungeon_auth` 实例 + 接线 `CMD_EXPEDITION`/`CMD_REQUEST_LAYOUT` + 重连快照纳入地牢状态。
- `dungeon_authority_test.gd`（11 用例）+ `session_root_test.gd`（现 22 用例）。

## 第八轮：Phase 7 酒馆共享操作 / 酿造锁（2026-07-12 02:0x，部署验证进行中）
用户说"继续"。
- 重写既有 `globals/multiplayer/tavern_authority.gd` 占位（原 extends Node TODO → extends RefCounted 纯逻辑）：
  - 共享金币仓库：`add_gold`/`spend_gold`(不足→INSUFFICIENT_RESOURCE)。
  - 共享材料仓库：`deposit_material`/`withdraw_material`(不足→INSUFFICIENT_RESOURCE)。
  - 酿造锁+生产队列：`start_brewing(peer,recipe,has_mats,has_gold)`（满→SERVER_BUSY、资源不足→INSUFFICIENT_RESOURCE）/ `complete_brewing` / `cancel_brewing`(仅所有者可取消→否则 PERMISSION_DENIED)；`serialize`/`deserialize`。
  - 注：真实材料/金币充足判定由 SessionRoot 从玩家真实 `ctx.inventory.materials` 与共享 `tavern_auth.gold` 判定后传入（与 combat/interaction 一致：权威只读真实状态、不信任客户端）。
- SessionRoot 加 `tavern_auth` 实例 + 接线 `CMD_BREW` 默认处理器（原子扣减背包材料+共享金币、广播 brewing_started）+ 重连快照纳入 tavern 状态（零行为变化）。
- `tavern_authority_test.gd`（12 用例）+ `session_root_test.gd` 加 3 个酿造集成用例（端到端消耗 / 缺材料拒绝 / 重连快照）。部署脚本加 `tavern_authority_test.gd`。
- 部署后台跑（8eaVTx，预期 19 套件 / 178 用例全绿）。

### 累计已落地（截至第八轮，纯逻辑 / 可 headless 单测）
PlayerRegistry、WorldState、CommandValidator(+SequenceTracker)、NetworkProtocol、CommandRouter、SnapshotReplicator、InteractionAuthority、LootAuthority、CombatAuthority、SessionRoot、MovementAuthority、EntitySyncAuthority、DungeonAuthority、TavernAuthority。
剩余 ⬜：Phase 8 断线/重连真实 RPC + 双进程 ENet 集成测试（需编辑器/CI）；真实场景发酵/升级/声望/顾客员工、按 seed 确定性重生成地牢、MultiplayerSpawner 节点生成 + 客户端插值 + 快照下发 RPC。

## 第九轮：范围纠正（用户："联机仅有地牢，只继承存档状态"）
- 用户纠正：联机仅地牢（出征）；玩家只继承各自单人存档；酒馆经营（酿造/升级/共享经济）为单人本地、不在联机范围。
- 回退第八轮错误范围：删 `globals/multiplayer/tavern_authority.gd`(+.uid)、`tests/gdunit/tavern_authority_test.gd`；session_root 移除 tavern_auth/CMD_BREW/_handle_brewing/tavern 快照段；network_protocol 移除 CMD_BREW/CMD_UPGRADE 及 EVT_TAVERN/EVT_BREWING 常量与 is_valid_* 列表项；部署脚本移除该套件。
- "只继承存档状态"接入点：`handle_spawn_request(peer_id, save_state:={})` 经 `_apply_save_state(inv, lo, save_state)` 把存档 materials/loadout 应用到新联机上下文（服务器可信数据，不信任客户端自报）。`session_root_test` 删 3 酿造用例、加 2 继承存档用例。
- docs/25 §5 顶部加范围修正 callout；§5.2 权威矩阵酒馆行、§21 步骤 11、链路说明改"不在联机范围"；附录 A 第 11 行标 ❌ 已撤销。

## 第十轮：部署验证 + 修两个真实缺陷，最终全绿
- 缺陷1（_apply_save_state API 误用）：EquipmentLoadout 正确 API 是 `from_dict(data)`/`get_weapon_slot(slot)`/`get_armor_slot(name)`（无 deserialize/get_weapon/get_armor）。测试同步改。另 `test_spawn_without_save_state` 原断言 str==0 错，init_defaults 默认全=5，改 5。
- 缺陷2（procedural_dungeon.gd 真实编译错误）：`ProceduralDungeon` 类 192 行 `_player_spawn = runtime.spawn_player()` 未声明 var（GDScript 2.0 硬错误）。该错误在 session_root.gd 比全局类缓存(`.godot/global_script_class_cache.cfg`)新时触发全项目脚本重扫描暴露，导致所有 preload session_root 的测试集体 PARSE FAIL（上游错误转嫁）。补 `var _player_spawn` 类级声明后根除（单测 network_protocol_test 复绿验证）。
- **部署结果 DEPLOY RESULT: PASS=18 FAIL=0**（18 套件全绿：session_root 24 / combat_bridge 37 / attr_panel 17 / movement 12 / entity_sync 11 / dungeon 11 / 其余小套件）。Orphan 计数仅 gdUnit 目录内其它测试文件计入，不参与判定。
- 累计已落地（仅地牢范围，纯逻辑/可 headless 单测/全实跑验证）：PlayerRegistry、WorldState、CommandValidator(+SequenceTracker)、NetworkProtocol、CommandRouter、SnapshotReplicator、InteractionAuthority、LootAuthority、CombatAuthority、SessionRoot(+继承存档)、MovementAuthority、EntitySyncAuthority、DungeonAuthority。
- 剩余 ⬜（需场景/RPC 接线，风险更高，需编辑器/CI 双进程验证）：Phase 8 断线/重连真实 RPC + ENet 集成测试、真实 MultiplayerSpawner 节点生成、客户端插值、按 seed 确定性重生成地牢接线。

## 第十一轮：Phase 8 断线/重连逻辑层落地并验证（§13）
- 新增 `globals/multiplayer/connection_authority.gd`（RefCounted 纯逻辑）：per-peer ONLINE/GRACE/LEFT 状态机。
  - `register_online(peer,guid,now)`：用 player_guid 锚定重连身份（§14.2，peer_id 每次连接会变）。
  - `on_disconnect(peer,now)`：→GRACE，签发确定性重连 token（`("%d|%f|%s"%[peer,salt,SECRET]).sha256_text()`），token_expiry=now+GRACE_PERIOD(60s)。
  - `resume`/`validate_reconnect`：错 token→`RECONNECT_TOKEN_INVALID`、now>tokExpiry→`RECONNECT_TOKEN_EXPIRED`、未知 peer→`RECONNECT_PEER_UNKNOWN`。
  - `touch`/`check_timeout`：心跳超时 HEARTBEAT_TIMEOUT=15s 判掉线。
  - `collect_expired_grace(now)`：GRACE 超时清理；`should_end_room_on_disconnect`：房主断线结束房间（第一版）。
- `session_root.gd` 集成：spawn 时 `connection_auth.register_online`；新增 `handle_peer_disconnected`/`handle_peer_left`/`handle_resume`(CMD_RESUME)/`heartbeat`/`tick_connections`；新增可控服务器时钟 `var current_time`（默认 0，断线/重连/清理用其作 now，单测直接赋值确定性验证）；`on_command` 加 GRACE 守卫（非 CMD_RESUME 且非 online→`ERR_INVALID_STATE`）；重连重置该 peer 序列(`reset_peer`)并下发 `session_snapshot`(EVT_SESSION_SNAPSHOT)。
- `interaction_authority.gd`：新增交互锁 `_interaction_locks` + `acquire_lock`/`release_lock`/`clear_locks_for(peer)`，交互成功时 `acquire_lock(eid, peer_id)`，断线由 `clear_locks_for` 释放（§13.1 释放交互锁）。
- `network_protocol.gd`：新增 `CMD_RESUME`/`EVT_SESSION_SNAPSHOT`/`ERR_RECONNECT_TOKEN_INVALID|EXPIRED|PEER_UNKNOWN`，同步 `is_valid_command`/`is_valid_event`。
- 新增 `connection_authority_test.gd`（20 用例）+ `session_root_test.gd` 加 9 个连接生命周期集成用例（断线保留上下文/重连下发快照并重注册/错 token 拒绝/超时拒绝/GRACE 期阻断命令/超时清理/房主断线结束房间/断线释放交互锁）。
- **部署结果 DEPLOY RESULT: PASS=19 FAIL=0**（connection_authority 20 / session_root 32 / combat_bridge 37 / attr_panel 17 / dungeon 11 / entity_sync 11 / movement 12 / 其余小套件全 0）。Orphan 计数仍不参与判定。
- 累计已落地（仅地牢范围，纯逻辑/可 headless 单测/全实跑验证）：+ ConnectionAuthority。
- 剩余 ⬜：真实 NetworkManager（SceneMultiplayer+ENet）RPC 接线调用本逻辑层、双进程 ENet 集成测试、客户端插值、按 seed 确定性重生成地牢场景接线——均无法纯 headless 验证，需编辑器/CI 双进程。

### 经验教训（重要）
- 全项目脚本重扫描触发条件：`session_root.gd` 等 .gd 的 mtime 晚于 `.godot/global_script_class_cache.cfg` → Godot 重扫全部脚本 → 任一脚本的 PARSE 错误会转嫁污染所有 preload 它的测试（报"Identifier X not declared in the current scope"但 X 在无关文件里）。排查时先用单测某无关套件（如 network_protocol_test）验证是否全扫描毒，再定位真正出错的源文件。
- EquipmentLoadout API：`from_dict(data)`/`to_dict()`/`get_weapon_slot(i)`/`get_armor_slot(name)`；attr_panel.init_defaults() 默认 str/dex/mag/con/agi/per 全=5。

## 第十二轮：NetworkManager 真实 RPC/传输层接线到纯逻辑层（联机栈跨入「可联网」）
- 改造既有 autoload `globals/core/network_manager.gd`（原仅 ENet host/join 占位 + 空 `request_player_spawn` stub）：
  - 持有 `SessionRoot` 权威实例：`host()`→`session.init_server()`，`join()`→`session.init_client()`；headless 单测经 `_ensure_session()` 无树初始化（`session = SessionRootClass.new()`）。
  - 服务器 RPC 接收端（any_peer）：`rpc_client_spawn/command/resume/heartbeat/leave`，均校验 `multiplayer.get_remote_sender_id()==peer_id` 防越权 → 调 `_server_handle_*` 接缝 → `SessionRoot` 裁决 → `_dispatch_event` 广播。
  - 客户端 RPC 接收端（authority）：`rpc_server_event`（→`_apply_event`，重连快照走 `apply_session_snapshot` 落地本地缓存）+ `rpc_server_spawned`（缓存重连 token/guid 供 `send_resume`）。
  - `_dispatch_event`：仅 `session.is_server && _can_rpc()` 时走 `rpc_server_event.rpc(event)`；headless（无真实 peer）只发 `event_dispatched` signal 供断言（避免 headless 调 RPC 崩溃）。
  - `tick(delta)`：服务器时钟 `session.current_time += delta` + `tick_connections` 清理过期 GRACE + 遍历 `connection_auth.online_peer_ids()` 心跳超时触发断线。
  - 客户端上送：`send_spawn/command/resume/heartbeat/leave`（`rpc_*.rpc_id(1, ...)` 仅 `is_client()` 时）。
  - `host/join/disconnect_session` 全部 `multiplayer` 访问加 `is_instance_valid(multiplayer)` 守卫：detached 节点 headless 下 `multiplayer` 为 null，跳过真实 peer 赋值仍可验证标志位（修复既有 `network_manager_test.gd` 2 失败）。
- `connection_authority.gd` 新增：`issue_token(peer,now)`（spawn 时预签发**稳定**重连 token，使断线重连沿用同一 token，避免 spawn/disconnect 两次派生不一致）；`online_peer_ids()`（心跳超时扫描）；`on_disconnect` 复用已签发 token（不再重新派生）。
- 新增 `tests/gdunit/network_manager_integration_test.gd`（8 用例：spawn 建上下文+签发 token+继承存档 / 命令路由广播+权威积分移动 / 重连恢复在线+下发快照 / token 过期拒绝 / 房主断线结束房间 / tick 清理过期 GRACE / 客户端应用快照 / GRACE 期阻断命令）。
- `connection_authority_test.gd` 加 3 例（token 稳定/未知空/online 列表）。
- 部署脚本 `tools/run_multiplayer_tests.sh` 纳入 `network_manager_test`/`network_manager_integration_test`。
- **验证结果**：`network_manager_integration_test` 8/0/0、`connection_authority_test` 23/0/0、`network_manager_test` 5/0/0；回归 `session_root`(32)/`interaction`(4)/`movement`(12)/`entity_sync`(11)/`dungeon_auth`(11)/`loot`(4)/`combat_auth`(4)/`command_router`(5)/`command_validator`(7)/`player_registry`(6)/`world_state`(5)/`snapshot_replicator`(3) 全 0 错误 0 失败。
- **累计联机纯逻辑层 + RPC/传输桥接层（仅地牢范围）全部落地并实跑验证**。剩余 ⬜：① 双进程 ENet 集成测试（真起两 Godot 进程）② 真实 `MultiplayerSpawner` 玩家/实体节点生成 ③ 客户端插值 ④ 按 seed 确定性重生成地牢场景接线。这些均需编辑器/双进程验证，无法纯 headless 证明。

### 经验教训（本轮）
- `NetworkManager` 是 autoload 但单测里 `NETWORK_MANAGER.new()` 是 detached 节点，`get_tree()==null` → `multiplayer` 为 null。凡在 host/join/disconnect/_dispatch_event 中访问 `multiplayer` 必须 `is_instance_valid` 守卫，否则 headless 单测 `Invalid access ... on a base object of type 'null instance'`。真实游戏里 autoload 在树中 `multiplayer` 有效，守卫不影响运行。
- `is_server` 是 `SessionRoot` 的属性，不是 `NetworkManager` 的；`_dispatch_event` 里必须用 `session.is_server`，曾误写成裸 `is_server` 触发 PARSE 错误（"Identifier is_server not declared"）。

## 第十三轮：双进程 ENet 集成测试（真实两个 Godot 进程端到端验证 RPC 链）
- 新增 `tests/integration/mp_host.gd`+`.tscn`（服务器进程：真实 `NetworkManager.host()` 开 ENet listen → 写 `server_ready.txt` → 监听 `event_dispatched`，收到 `player_spawned` 校验 `session.registry.has_peer(peer) && is_spawned(peer)` → 写 `server_ok.txt`）。
- 新增 `tests/integration/mp_client.gd`+`.tscn`（客户端进程：真实 `join(127.0.0.1)` → 轮询 `NetworkManager.multiplayer.multiplayer_peer.get_connection_status()==CONNECTION_CONNECTED` → `send_spawn({}, "client_guid_001")` → 轮询 `reconnect_token!=""`（由 `rpc_server_spawned` 写入）→ 写 `client_ok.txt`）。
- 编排器 `tools/run_integration_test.sh`：先后起 server/client 两个独立 Godot 进程（独立 `APPDATA` 防 `user://` 竞争）、结果文件轮询 + 失败 dump 日志 + 结束清理。
- **关键坑（实录，避免再踩）**：
  1. 传给原生 Windows Godot 的 `APPDATA` 与场景路径必须是 **Windows 风格** `D:/123/...`；用 git-bash 风格 `/d/123/...` 会被 Godot 当 unix 路径去 `mkdir /d` → `Could not create directory: '/d'` → signal 11 崩溃。bash 自身读写仍用 `/d/123/...`。
  2. 本版本 `get_tree().multiplayer` 在 `SceneTree` 上**无效**（SCRIPT ERROR: Invalid access to property 'multiplayer' on SceneTree）；须用 `NetworkManager.multiplayer`（Node 属性，同一 SceneMultiplayer 实例）。
  3. 客户端须等 ENet `CONNECTION_CONNECTED` 后才 `send_spawn`，且 `local_peer_id` 由 `_on_connected_to_server` 异步设置，发前显式 `NetworkManager.local_peer_id = NetworkManager.multiplayer.get_unique_id()` 兜底。
- **实跑结果 `INTEGRATION TEST: PASS`**：服务器 `OK peer=83206123`、客户端 `OK token=17e8cfe8…`。证明完整 RPC 链客户端 `rpc_client_spawn` → 服务器权威 `handle_spawn_request` 建 `PlayerContext` → 服务器 `rpc_server_spawned`(确认+token) + `rpc_server_event`(广播→客户端 `_apply_event`) 在真实 ENet 下端到端打通。
- `.gitignore` 加 `.tmp_ittest/`/`.tmp_apdata_srv/`/`.tmp_apdata_cli/`。
- **联机栈「纯逻辑层 + 真实 RPC/传输层 + 双进程端到端」三层全部落地并验证（仅地牢范围）**。剩余 ⬜：② 真实 `MultiplayerSpawner` 玩家/实体节点生成与地牢场景接线；③ 客户端插值；④ 按 seed 确定性重生成地牢场景接线。均属表现层（把权威状态映射到可见节点），逻辑/传输地基已就绪。

## 第十四轮：表现层 ① 落地——远端玩家 avatar 经桥接层 RPC 复制 + 客户端插值（双进程验证 PASS）
用户"继续"推进 ①（把权威状态映射到可见场景节点）。

### 成果
- 新增 `globals/multiplayer/multiplayer_scene_bridge.gd`(表现层桥接,不声明 class_name) + `.tscn`(仅 `RemoteAvatars` 容器)、`scenes/multiplayer/multiplayer_avatar.gd`+`.tscn`(远端 avatar 节点:`Label3D` 名牌 + `apply_snapshot` 插值)、双进程测试 `tests/integration/mp_avatar_test.gd`+`.tscn` + 编排器 `tools/run_integration_avatar_test.sh`。
- **实跑 `INTEGRATION AVATAR TEST: PASS`**：服务器 `peer_authorized` → 本地生成远端 avatar + `rpc_spawn_avatar` 广播 → 客户端实例化（自身 peer 经 `_spawn_local` 守卫 `peer_id==_local_peer_id()` 跳过）；服务器 `rpc_snapshot` 驱动位移 → 客户端 avatar 插值跟随（x>0.3）。
- 架构：每个 peer 显示【远端玩家】为 avatar（服务器显客户端、客户端显服务器/其他客户端），本地玩家由真实 `Player` 控制器驱动，不触碰单人流程。

### 关键工程发现（本 Godot 4.7 构建）
1. **`MultiplayerSpawner` 在本构建不可靠**：`SceneTree` 无 `.multiplayer` 属性，其 `_setup_spawn()` 依赖 `get_tree().get_multiplayer()` 致 `spawn_path` 无法向已连接 peer 注册、复制永不触发。改用桥接层自带显式 RPC（`authority→call_remote`），功能等价且可控。
2. `MultiplayerSpawner.spawnable_scenes` **不是 GDScript 可读属性**（内部名 `_spawnable_scenes`，仅 `add_spawnable_scene()`/`get_spawnable_scene_count()` 方法可用）；`.tscn` 写 `spawnable_scenes=[...]` 同样因①无效。
3. 测试脚本 `get_tree().multiplayer` 无效 → 用 `NetworkManager.multiplayer`。
4. 启动器传原生 Windows Godot 的路径须 `D:/123/...`（git-bash `/d/123/...` 被当 unix 路径 → signal 11）。
5. 端口须带重试（14567 常被残留进程占，用 `[28999,29001,29234,29567,14567]`）。
6. 残留 Godot 进程本沙箱 `taskkill` 常杀不掉 → 靠独立 `APPDATA` + 端口重试规避。

### 待办（剩余 ⬜）
- ② 实体（敌人/掉落）节点生成可按本桥接 RPC 范式接入。
- ③ 真实 `CMD_INPUT`→`session`→`player_snapshot` 下发链路需地牢场景将玩家标 alive（与 `procedural_dungeon.gd` 接线）；插值机制本身已验证。
- ④ 按 seed 确定性重生成地牢场景接线（`DungeonAuthority` + 客户端 WFC 重生成校验）。

## 第十五轮：垂直切片——两个真实玩家进同一张真实地牢 + 服务器权威移动同步（双进程 ENet PASS）
用户设定最关键下一步 = "完成'两个真实玩家进入同一张真实地牢，并通过服务器同步移动'的集成测试"。**已完成并实跑验证 PASS**。

### 落地成果
- `globals/core/network_manager.gd` 增 3 法：`submit_command`（host 本地也走权威 `_server_handle_command`，client 走 `send_command`——统一命令入口，使房主自身输入也经权威积分）；`spawn_self(save_state,guid)`（服务器为自己建 PlayerContext）；`start_expedition(seed_hint)`（服务器起出征→`dungeon_auth.start_expedition`→`_dispatch_event` 广播 layout 事件）。
- `scenes/characters/player/player.gd` 增 `apply_remote_snapshot(pos,yaw)`：把服务器权威快照落到真实 Player 节点（global_position/rotation.y，velocity 清零）。
- 新增 `scenes/multiplayer/client_command_driver.gd`（extends Node，无 class_name）：每帧采集本地输入（`override_move` 优先用于 bot/测试，否则 `player.input_dir`；move.y 取反适配世界系）→ 组 `CMD_INPUT`（含 `type`/`world_revision`/`sequence`/`sprint`/`look_yaw`）→ `nm.submit_command`；监听 `event_received`/`event_dispatched`，仅对 `local_peer_id` 的 `EVT_PLAYER_SNAPSHOT` 设 `_target_position/_target_yaw` → 每帧 `apply_remote_snapshot`。另有 `send_attack/interact/skill`。**坑修**：`_server_world_revision` 用 `nm.session.world.world_revision`（world 是类型化属性，早期误写 `if "world" in nm.session` 崩）。
- 新增 `scenes/multiplayer/dungeon_session_controller.gd`（extends Node，无 class_name）：`build_and_enter(seed)` → `_build_real_dungeon()`（真实 `DungeonGenerator.generate(config{seed,zone=0})` → `DungeonSceneBuilder.build(layout,root)` + DirectionalLight）+ `_spawn_local_player()`（`player.tscn` 实例化到 spawn cell + 挂 ClientCommandDriver）；`_compute_player_spawn_pos()` 复刻 procedural_dungeon 地形数学；`layout_fingerprint()`=`"w|h|grid.hash|spawn_cell"` 确定性指纹。
- 新增 `tests/integration/mp_dungeon_test.gd`+`.tscn` + 编排器 `tools/run_integration_dungeon_test.ps1` + `.cmd` 启动器（`dungeon_srv.cmd`/`dungeon_cli.cmd`）。

### 实跑结果 `INTEGRATION DUNGEON TEST: PASS`
- `SERVER: OK seed=53700`；`CLIENT: OK local_x=0.267 remote_x=5.693 seed_match=true fp_match=true`。
- `DUNGEON SYNC`: host=`53700|42|42|1517005569|(36, 5)` == client 同指纹 → **两端同一张真实地牢**（同 seed→同 layout）。
- `MOVEMENT: OK`：客户端本地真实 Player 由服务器权威快照驱动 x 位移；远端 avatar（host）经桥接 RPC 复制并移动。

### 关键坑（本轮实录，避免再踩）
1. **`dungeon_layout` 事件用键 `"type"` 而非 `"event"`**（源自 `dungeon_auth.make_layout_event()`）——与其它所有事件都用 `"event"` 不同。客户端事件处理器须 `event.get("event", event.get("type", ""))` 两键都查，否则 layout 事件被丢弃 → 客户端 player=false 建不出地牢。
2. **PowerShell 5.1 `Start-Process` 环境变量 bug**：当 `PATH`/`Path`/`path` 大小写变体同时存在 → 抛"已添加项。字典中的关键字:'PATH'"。规避：**不用 `Start-Process`**，改写 `.cmd` 启动器用 `start "" /min cmd /c "Godot ... > log 2>&1"`，经 `& cmd.exe /c $CMD` 调起。
3. 从 Bash 工具调 PowerShell 被安全拦截 → 必须**直接用 PowerShell 工具**跑编排器。
4. 传原生 Windows Godot 的 APPDATA/路径仍须 `D:/...` 风格（沿用前轮）；端口重试 `[28999,29001,29234,29567,14567]`；独立 APPDATA（`.tmp_apdata_dungeon_srv`/`_cli`）。

### 待办（剩余 ⬜，按用户优先级）
- ② 敌人/箱子/门/陷阱/传送门实体节点经桥接 RPC 生成（`scenes/multiplayer/multiplayer_entity.gd`+`.tscn` 已建，尚未接 `EntitySyncAuthority`/`SessionRoot.set_entity` 与桥接层）。
- ⑤ 掉落实体复制；⑦ 真实攻击命中（client `send_attack`→`CombatAuthority`→敌人 HP→`entity_snapshot`）；⑧ 出征结算写回单人存档；⑨ 断线/重连真实场景恢复（逻辑已成，场景验证待做）；⑩ Lobby UI。
- 架构债：`dungeon_session_controller.gd` 为独立切片控制器，最终应并入真实 `procedural_dungeon.gd` 入口流程，使正式游戏与测试走同一路径。

## 第十六轮：② 敌人实体节点经桥接层复制到两端场景（双进程 ENet PASS）
用户"都要完成，逐步推进"。从优先级 ② 起步。

### 关键设计洞察
- 实体事件（`entity_spawned/entity_snapshot/entity_despawned`）**已走正常事件管线**：服务器 `_dispatch_event`→`event_dispatched` signal + `rpc_server_event`→客户端 `_apply_event`→`event_received` signal。桥接层 `_on_event` 已同时连这两个 signal。**故实体复制无需像 avatar 那样单独 RPC**——桥接层两端各自在 `_on_event` 里生成/更新/销毁可见节点即可。（avatar 需单独 RPC 是因为 `peer_authorized` 只在服务器触发。）
- 实体事件结构：`{"event": EVT_ENTITY_SPAWNED, "entity_id": id, "data": {...}}`；`data` 是 `set_entity(id,data)` 存的原始字典（kind/position/current_life/max_life/label）。实体事件用 `"event"` 键（**非** dungeon_layout 的 `"type"` 键）。

### 落地
- `globals/multiplayer/multiplayer_scene_bridge.gd`：加 `entity_scene`(preload multiplayer_entity.tscn) + 运行时建 `RemoteEntities` 容器 + `_entities` 字典；`_on_event` 加三分支 → `_spawn_entity_local`(幂等,已存在改走更新,供重播追平不产生重复节点)/`_update_entity_local`(节点缺失兜底 spawn,防 snapshot 早于 spawn)/`_despawn_entity_local`(调 apply_despawn)；加 `get_entity_node(id)`/`entity_count()` 查询。**坑**：multiplayer_entity 用 `@onready` 标签节点 → 必须先 `add_child` 再 `apply_spawn`。
- `globals/core/network_manager.gd`：加 `server_spawn_entity/server_update_entity/server_despawn_entity`（调 `session.set_entity/update_entity/remove_entity` + `_dispatch_event` 广播）+ `rebroadcast_entities()`（遍历 `session.all_entities()` 重播 entity_spawned,供晚到客户端追平,幂等）。
- `globals/multiplayer/session_root.gd`：加 `all_entities()` getter（返回 `_entities`）。
- `scenes/multiplayer/dungeon_session_controller.gd`：加 `spawn_server_entities()`（仅 `nm.is_host`；出生点偏移放 2 只敌人 Rat(hp12)/Skeleton(hp20)，经 `nm.server_spawn_entity` 广播）。
- `tests/integration/mp_dungeon_test.gd`：host build 后 `spawn_server_entities`；`_on_host_peer_authorized` 加 `nm.rebroadcast_entities()`（客户端晚连,须重播）；client 加断言 4——轮询 `_bridge.get_entity_node(1001/1002)!=null` + 抽查 kind/hp/pos 一致。verdict 纳入 `ent_ok`。
- `tools/run_integration_dungeon_test.ps1`：verdict 加 `ENTITIES` 段 + PASS 条件加 `ent_ok=True`。

### 实跑 PASS
`SERVER: OK seed=28526 entities=2`；`CLIENT: OK entities=2`；`ENTITIES: ent_ok=true count=2 detail=kind=enemy hp=12/12 pos=(-21.0,0.5,-27.0)`。两只服务器权威敌人经桥接层复制到客户端场景,位置/HP 一致。移动/地牢同步回归仍 OK。

## 第十七轮：⑦ 真实攻击命中（双进程 ENet PASS）
用户"都要完成，逐步推进"，② 之后做 ⑦。

### 落地
- `globals/multiplayer/session_root.gd`：`_handle_combat` 在结算后把伤害**写回实体注册表**——`defender_life<=0` 调 `remove_entity`(广播 entity_despawned，敌死移除) 否则 `update_entity({"current_life":new_life})`(广播 entity_snapshot，扣血可见)；死亡路径先调 `_on_entity_killed`(本轮回填) 再 remove。`return {"success":true,"event":out["event"],"error_code":"","extra_events":extra}`——extra 含 entity 复制事件。
- `globals/core/network_manager.gd`：`_server_handle_command` 扩展分发 `res["extra_events"]` 数组（每元素 `rpc_server_event` 广播），战斗扣血/死亡/掉落事件由此下发。
- 测试敌人给 `con:0,armor_def:0`（str=5 → ~11/击，12HP Rat 两击死，确定性可见扣血）。
- `tests/integration/mp_dungeon_test.gd`：host `spawn_server_entities` 2 敌(Rat1001/Skeleton1002)；client 加断言 5——`send_attack(1001)` → 轮询敌死(节点移除) + 抽查封中间扣血(`hp_dropped`) + 校验 1002 存活；verdict 纳入 `combat`。
- `tools/run_integration_dungeon_test.ps1`：verdict 加 `COMBAT` 段 + PASS 条件加 `combat_ok=True`。

### 实跑 PASS
`SERVER: OK seed=965799 entities=2`；`CLIENT: OK ... entities=2 combat=true`；`COMBAT: combat_ok=true start_hp=12 hp_dropped=true other_alive=true`；`INTEGRATION DUNGEON TEST: PASS`。攻击→服务器权威扣血→快照/死亡事件两端同步闭环。

## 第十八轮：⑤ 掉落实体复制 + 拾取（双进程 ENet PASS）
②+⑦ 之后，按"逐步推进"做 ⑤：敌人死亡→确定性掉落→掉落实体复制→玩家拾取→两端移除。

### 关键设计
- 实体事件（entity_spawned/snapshot/despawned）已走正常事件管线（`_dispatch_event`→`rpc_server_event`→两端 bridge `_on_event`），**无需单独 RPC**（同 ② 结论）；avatar 才需单独 RPC。
- 掉落实体 `kind="loot"`，数据含 `item_id/item_kind/amount/position`（供 `InteractionAuthority.resolve_interaction` 消费）；桥接层 `multiplayer_entity` 节点按 kind 显示标签、`max_hp=0` 故无血条。

### 落地
- `session_root.gd`：实现 `_on_entity_killed(entity_id, killer_peer)`（此前为占位返回 `[]`）：
  - 读敌人 `loot_table` → `server_roll_loot(table, 确定性种子, 4)` 掷骰（种子 = `entity_id*2654435761 + _loot_seq`，可重连回放）；
  - 为每个掉落物分配 `_loot_seq`(自 5000 起) 唯一 id，掉落位置取**击杀者服务器权威坐标**（保证击杀者在交互范围内）；
  - 每物 `set_entity(lid, {kind:loot,label,position,item_id,item_kind,amount,current_life:0,max_life:0,consumed:false})` → 产出 entity_spawned，由 `_handle_combat` 并入 extra_events 广播。
- `_handle_interaction` 改造：拾取成功(`resolve_interaction` 返回 success)后 `remove_entity(target)` 并把 `entity_despawned` 事件并入 `res["extra_events"]` → 两端 bridge 移除可见掉落物节点。**行为变更**：拾取现从权威注册表彻底移除实体（不再仅标记 consumed）。
- `client_command_driver.gd`：`send_interact` 改发 `target_entity_id`（交互权威读取的键；战斗权威读 `target_hint`，二者不同）并保留 `target_hint` 兼容——修复了此前 `send_interact` 只发 `target_hint` 导致交互必失败的隐性 bug。
- `dungeon_session_controller.gd`：`spawn_server_entities` 给 Rat(1001) 加 `loot_table={"goblin_tooth":{"kind":"material","weight":10,"min":1,"max":2}}`（高权重保必掉，测试断言用）。
- 纯逻辑单测 `session_root_test.gd`：`test_interaction_pickup_end_to_end` 二次拾取断言由 `ERR_TARGET_ALREADY_CONSUMED` 改为 `ERR_INVALID_TARGET`（实体已被移除，符合新行为）。
- 集成测试 `mp_dungeon_test.gd`：战斗后 `_do_loot=true` 冻结本地移动（停在击杀点附近），扫描 bridge `_entities` 中 `kind=="loot"` 节点 → `send_interact(loot_id)` → 轮询该节点消失验证复制。`run_integration_dungeon_test.ps1` 加 `LOOT` 段 + PASS 条件 `loot_ok=True`。

### 实跑 PASS（双进程 ENet）
`SERVER: OK seed=524027 entities=2`；`CLIENT: OK ... loot=true`；`LOOT: loot_ok=true appeared=true picked=true id=5001`；`INTEGRATION DUNGEON TEST: PASS`。
纯逻辑回归：`session_root_test` 32/0/0、`interaction_authority_test` 4/0/0、`loot_authority_test` 4/0/0 全绿。

### 累计已落地（垂直切片 ②+⑤+⑦ 全闭环）
两个真实玩家进同一真实地牢 → 服务器权威移动同步 → 敌人经桥接层复制到两端 → 真实攻击命中扣血/死亡 → 敌人死亡确定性掉落 loot 实体并复制到两端 → 玩家拾取 → 掉落物从两端世界移除。剩余 ⬜：⑧ 出征结算写回单人存档；⑨ 断线/重连真实场景恢复（逻辑已成，场景验证待做）；⑩ Lobby UI；⑪ 性能；⑫ Dedicated Server；架构债（dungeon_session_controller 并入 procedural_dungeon.gd 入口）。

## 第十九轮：⑧ 出征结算写回单人存档（实现+纯逻辑 PASS，集成验证中）
②+⑤+⑦ 之后，按"逐步推进"做 ⑧：联机出征结束 → 服务器权威结算玩家【本次净获得】→ 写回该玩家各自的单人存档（GameState.expedition_inventory）+ 持久化。

### 关键设计
- 联机仅地牢、酒馆经济为单人本地：出征结算**不汇入 TavernManager**（与单人 `DungeonRuntime._settle_extraction_loot` 的 deposit 行为不同），而是写回玩家各自单人存档（符合"玩家只继承各自单人存档"）。
- **净获得结算（delta）**：spawn 时记录 `_inventory_baseline[peer_id]=inv.to_dict()`；出征结算 `_compute_settlement` = 当前 `ctx.inventory` − 基线，按 materials/runes/equipment 三类求正差值。优点：不重复累加带入地牢的基线物资、不丢失玩家单人存档既有符文/装备（基线/结算都不含它们）。
- `EVT_EXTRACTION_RESULT` 含 `peer_id` + `settlement`；桥接层 `_on_event` 仅当 `event.peer_id == _local_peer_id()` 时写回——保证各自只写自己的单人存档（host 写 host、client 写 client，互不串）。

### 落地
- `network_protocol.gd`：加 `EVT_EXTRACTION_RESULT := "extraction_result"`，并加入 `is_valid_event` 列表；`CMD_EXTRACT` 常量已存在。
- `session_root.gd`：新增 `_inventory_baseline` 字典（_init 初始化、unregister 擦除）；`handle_spawn_request` 在 `_apply_save_state` 后记录基线；`wire_default_authorities` 注册 `CMD_EXTRACT→_handle_extract`；实现 `_handle_extract`（返回 EVT_EXTRACTION_RESULT）与 `_compute_settlement`（三类正差值）。
- `client_command_driver.gd`：加 `send_extract()`（CMD_EXTRACT，复用 `_server_world_revision`+序列号）。
- `multiplayer_scene_bridge.gd`：`_on_event` 处理 `EVT_EXTRACTION_RESULT`（guard peer_id==local）→ `_apply_extraction_settlement`：把 settlement 三类净获得分别 `add_material/add_rune/add_equipment` 进本地 `GameState.expedition_inventory`，并 `SaveManager.save_to_slot(0)` 持久化（垂直切片固定槽位 0，生产应接当前存档槽位）。
- 集成测试 `mp_dungeon_test.gd`：战斗+拾取后加断言 7——`_driver.send_extract()` → 轮询本地 `GameState.get_carried_materials_dict().has("goblin_tooth")` 验证净获得写回 → 写 `client_extract.txt`。`run_integration_dungeon_test.ps1` 加 `EXTRACT` 段 + PASS 条件 `extract_ok=True`。
- 纯逻辑单测 `session_root_test.gd`：增 3 例——`test_extraction_settlement_returns_net_gain_only`(基线 rat_tail=5 不回写、仅 goblin_tooth=2)、`test_extraction_handler_emits_result_event`(事件+runes 结算)、`test_extraction_settlement_zero_when_nothing_gathered`。

### 验证（最终）
纯逻辑：`session_root_test` 35/0/0（含 3 例新 ⑧，全 PASSED）。
双进程 ENet 集成测试 **PASS**（seed=519176）：`CLIENT: OK ... extract=true`；`EXTRACT: extract_ok=true`（client_extract.txt 确认本地 `GameState.expedition_inventory` 含 `goblin_tooth` 净获得写回 + `SaveManager.save_to_slot(0)` 持久化）。
完整闭环：② 敌人复制 + ⑦ 真实攻击 + ⑤ 掉落实取 + ⑧ 出征结算写回单人存档 **全部 PASS**。

## 第二十轮：⑨ 断线/重连真实场景恢复（triple-process ENet 集成测试 PASS）
②⑤⑦⑧ 之后，按"逐步推进"做 ⑨：晚到/重连客户端须重建真实地牢 + 追平会话中既有玩家 avatar + 实体 + 服务器权威移动同步。

### 关键设计（机制，此前已落地，本轮验证）
- 晚到客户端经 `EVT_DUNGEON_LAYOUT`(键 "type") 重建真实地牢；`EVT_SESSION_SNAPSHOT` 用 `session_root.build_session_snapshot()`（含 players/entities/dungeon）追平。
- 桥接层 `_on_peer_authorized` 向新授权 peer 重发所有已存在 peer 的 avatar（`rpc_spawn_avatar.rpc_id`）；`_apply_session_snapshot` 同时 spawn 实体与 player avatar（跳过本地 peer）。

### 诊断根因（重要 gotcha，避免回归）
- 集成测试判定 `fp_match` 用 `host_line.get_slice("|",1)` 比较——但 **GDScript `String.get_slice(sep, i)` 返回第 i 个字段（仅 "42"），而非"第 i 个分隔符之后的完整子串"**。需取首个 `|` 之后的**完整地牢指纹**须用 `host_line.split("|", true, 1)[1]`。`get_slice("|",1)` 只取宽度字段，导致与完整 `layout_fingerprint()` 比较恒 false。
- 主客户端此前 `fp_match=true` 纯属巧合：两边**都用** `get_slice("|",1)` 比较，都取到 "42" 而相等（其实只比了宽度，逻辑也错）。晚到 client2 一端用 `get_slice` 一端用完整指纹 → 恒 false。
- 另一断言 bug：`peer1_av = get_avatar(2)` 写死 peer id=2，但 ENet 动态分配 peer id（如 1594324783）。改动态查找"非房主(1)非自身"的远端 avatar（`get_avatar_peers()` 遍历）。
- 真实功能其实早正确：日志证实 client2 收到 layout/snapshot/entity_spawned 并正确生成 host+client1 两个远端 avatar（`after snapshot: avatars=[1594324783, 1]`）；`entities=1` 也正确（先到 client1 已击杀敌 1001）。

### 落地修复
- `multiplayer_scene_bridge.gd`：加公开 `get_avatar_peers()`（测试动态查远端 avatar）。
- `mp_dungeon_test.gd`：`_read` 加 `_host_fingerprint()` 用 `split("|",true,1)[1]` 取完整指纹；recovery 检查 `peer1_av` 改动态查找；加 `[CLIENT2-REC]` 调试打印定位 fp_match。
- 生产代码诊断打印清理：`network_manager.gd`(rpc_server_event/_dispatch_event) 与 `multiplayer_scene_bridge.gd`(snapshot/avatar rebroadcast) 的 `print()` 已全部移除（仅 cosplay，未触动逻辑）。

### 实跑 PASS（triple-process：host + client1 跑完②⑤⑦⑧ + 迟到 client2 验证⑨）
`RECOVERY: ok=true recovered=true both_avatars=true host_av=true peer1_av=true entities=1 fp_match=true peer_moved=true`
`INTEGRATION DUNGEON TEST: PASS`（seed=649124）。
**垂直切片 ②+⑤+⑦+⑧+⑨ 全部闭环 PASS。** 剩余 ⬜：⑩ Lobby UI；⑪ 性能；⑫ Dedicated Server；架构债（dungeon_session_controller 并入 procedural_dungeon.gd）。

## 第二十一轮：⑩ 联机大厅 UI（headless 冒烟 PASS + 架构债部分消解）
按"推进"做 ⑩：主菜单「联机」按钮 → 大厅 UI → 真实进入地牢。同时把集测里内联的"进入真实地牢"逻辑抽到共享 autoload，消解双份实现漂移（架构债部分消解）。

### 新增/修改文件
- `globals/multiplayer/multiplayer_session.gd`（**新 autoload**，无 class_name，已注册 project.godot:26）：host_room/join_room/start_expedition/leave_room + `_on_peer_authorized`(晚到重播复用⑨逻辑) + `_ensure_bridge`(常驻桥接节点) + `connected_peers()` 透传 + signals(dungeon_entered/room_updated/connection_failed/host_failed)。
- `scenes/ui/lobby_menu.gd` + `lobby_menu.tscn`（**新**）：Host/Join/Lobby 三面板，纯代码建节点；按钮→MultiplayerSession；dungeon_entered 后 queue_free 让 3D 接管。引用 `lantern_theme.tres`(存在)。
- `scenes/ui/main_menu.tscn`：MenuVBox 在 SettingsBtn 后插入 MultiplayerBtn；`main_menu.gd`：加 `@onready multiplayer_btn` + connect + `_on_multiplayer_pressed`(change_scene_to_file) + `_update_button_texts` 加 tr("Multiplayer")。
- `scenes/ui/localization/translations.csv`：补 8 键（Multiplayer/Multiplayer Lobby/Host Room/Max Players/Create Room/Join Room/Server Address/Connected Players）。
- `tools/_smoke_lobby.gd`（**新** 冒烟）：-s headless 实例化 lobby→host_room→start_expedition→dungeon_entered 验证后端接线。

### 修复的 3 个真实 bug（冒烟暴露）
1. `multiplayer_session._ready` 内 `root.add_child(bridge)` 报 "Parent node is busy setting up children" → 改 `get_tree().root.call_deferred("add_child", bridge)`（autoload _ready 期间 root 在构建子节点，须延迟挂树）。
2. `lobby_menu` 用 `node.theme_override_constants["separation"]=` / `theme_override_font_sizes["font_size"]=` → 4.7 无此属性报错 → 全改 `add_theme_constant_override("separation", N)` / `add_theme_font_size_override("font_size", N)`。
3. 大厅 UI 调 `ms.connected_peers()` 但 MultiplayerSession 无该方法 → 加透传（NetworkManager.connected_peers()）。

### 验证 PASS（headless 冒烟 LOBBY_SMOKE_RESULT: PASS）
`lobby instantiated child_count=3`；`MultiplayerSession present, bridge=yes`；`host_room OK peers=[1]`；`start_expedition OK seed=164932 is_in_dungeon=true`。**⑩ 完成。**

### gotcha 记录（勿回归）
- autoload `_ready` 内 `add_child` 到 root 须 `call_deferred`（同上 1）。
- UI 主题覆盖**只**能用 `add_theme_*_override`，不可用 `theme_override_*` 索引赋值（4.7 无该属性）。
- 冒烟用 PowerShell 须 `2>&1 | Out-File`（管道），**勿 `>` 文件重定向**（会吞 Godot 全部 stdout，EXIT=0 但空日志）；跑前 `taskkill /F /IM Godot_*.exe` 清残留（残留进程会锁日志文件导致 Out-File 失败）。
- 架构债部分消解：真实游戏(大厅UI)与集成测试现共用 MultiplayerSession 进入路径；DungeonSessionController 仍作为构建器被 MultiplayerSession 持有（终极并入 procedural_dungeon.gd 待做）。

## 第二十二轮：⑪ 网络带宽节流 + PerfMonitor HUD（三进程集成测试 PASS）
按"逐步进行"推进 ⑪ 性能。

### 根因
客户端每物理帧(60Hz)发 CMD_INPUT → 服务器 `_server_handle_command` 每收到一帧就立即广播 `EVT_PLAYER_SNAPSHOT` → client2 日志曾记录 ×1593 条快照（≈60Hz×2人），为联机带宽大户。

### 落地
- `globals/core/network_manager.gd`：新增常量 `SNAPSHOT_BROADCAST_HZ := 30.0`；`player_snapshot` 事件在 `_has_real_peers()` 下改缓冲进 `_snapshot_buffer[peer_id]`（最新一帧覆盖），`tick(delta)` 调 `_flush_snapshots(delta)`：`_snapshot_accum += delta`，达 `1/SNAPSHOT_BROADCAST_HZ` 才遍历 `_snapshot_buffer` 取最新一帧经 `_dispatch_event` 下发。单进程(无真实 peer)仍走即时 `_dispatch_event`，不破 headless 单测。
- 网络统计：加 `_net_total/_net_window_*` 计数 + `get_net_stats() -> Dictionary` + 每 ~5s 打印 `[NETSTATS]`（含 player_snapshot/entity_spawned/combat_resolved 等分类计数）。
- `globals/perf/perf_monitor.gd`（**新 autoload**，注册 project.godot，F3 切换 HUD）：显示 FPS/帧耗时 + RenderObjs(`Performance.RENDER_TOTAL_OBJECTS_IN_FRAME`)/Primitives + 网络 msg/s(`NetworkManager.get_net_stats()`)。**避坑**：4.7 的 `RenderingServer.get_rendering_info` 不再接受 2 参数、`RENDERING_INFO_TOTAL` 枚举已移除 → 改用 Performance 单例稳定常量（已删脆弱的 `_read_draw_calls()`）。

### 验证 PASS（三进程 ENet 集成测试，日志 `.ittest_dungeon_*.log`）
- **节流生效**：client2 收到 `EVT_PLAYER_SNAPSHOT` **114** 条（vs 基线 1593），约降 93%，与 ~30Hz 节流一致（114 ≈ 30Hz × 数秒活跃对局）。
- **稳定性**：三进程日志（server/client/client2）**0 个 SCRIPT ERROR**。
- **功能回归全绿**：最终回合 `INTEGRATION DUNGEON TEST` 末行 `seed=350285 ... recovery=ok=true recovered=true both_avatars=true host_av=true peer1_av=true entities=1 fp_match=true peer_moved=true` —— ② 敌人复制 / ⑤ 掉落拾取 / ⑦ 真实攻击 / ⑧ 出征结算 / ⑨ 断线重连 在节流下全部 PASS。
- server `[NETSTATS]` 末行：`player_spawned=2, dungeon_layout=2, entity_spawned=5, player_snapshot=169, combat_resolved=2, entity_despawned=2, interaction_result=1, extraction_result=1`。

### 结论
⑪ 完成：服务器权威快照广播由 60Hz 降至 30Hz（缓冲最新帧，无遥测丢失），联机带宽大幅下降且全部功能回归通过。PerfMonitor HUD 已就绪（F3 在真机可见）。
剩余 ⬜：⑫ Dedicated Server；架构债（DungeonSessionController 并入 procedural_dungeon.gd 入口统一）。
