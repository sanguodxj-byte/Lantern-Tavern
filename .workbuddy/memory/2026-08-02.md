# 2026-08-02

- 指定并落地符文图标规范：最终 128×128 RGBA8，先在 32×32 逻辑栅格绘制，再以最近邻整数放大；透明底、2px 逻辑主笔画、2px 安全区、最多轮廓/主色/高光三层，禁抗锯齿、渐变、软发光与字体直绘。规范写入 `docs/19-符文系统（构筑深度）.md` §3.2，术语表新增“符文图标”。
- 重写 `data/rune_glyphs.gd` 为纯 CPU、确定性、按五类语义骨架生成的像素绘制器；运行时继续由 `EquipmentDetailPopup.icon_for_rune()` 接线，不依赖外部 PNG 主体。
- 扩展 `rune_glyphs_test.gd` 覆盖 128px 尺寸、32px 逻辑格式、透明安全边、无半透明像素、4×4 最近邻块、颜色、缓存尺寸键、未知 ID 占位与确定性；10/10 通过。
- `tools/equipment_ui_icon_capture.gd` 新增全部 50 枚符文的 128px 图鉴板，产物 `reports/ui_preview/rune_codex_128px.png`（908×1454 RGBA PNG），用于视觉验收。
- 根据用户反馈将符文从“透明底单符号”升级为完整像素卡面：切角透明、暗色石板纹理背景、深描边+金属双层边框、四角铆钉、五大家族专属框饰和中央符文。契约测试更新为 11/11 通过，包含卡面中心不透明、背景与边框分层检查。
- 新增独立符文之语图标系统 `data/rune_word_glyphs.gd`：33 枚之语采用 128×128 华丽八角圣匣卡面、三层金属框、稀有度材质、主题冠饰、六类复合徽记与按配方顺序排列的彩色宝石；通过 `EquipmentDetailPopup.icon_for_rune_word()` 接口提供 UI。规范写入文档 §3.3，图鉴 `reports/ui_preview/rune_word_codex_128px.png`，契约测试 7/7 通过且确认完整卡面两两不同。
- 完成只读架构审查，报告 `reports/architecture_review_2026-08-02.md`。主要待治理项：Android renderer 被设为 forward_plus 且未独立分档；酒馆装备面板仍引用已迁出 GameState 的库存常量；本地攻速公式与联机固定 0.4s 冷却分叉；地牢/敌人表现仍回退 `GameState.current_player`，per-peer 迁移未闭环。Godot 4.7 headless 编辑器扫描通过；当前工作树存在大量未整理变更，后续应从干净基线验证。
- 重绘战斗 HUD 生命、蓝量与两类护盾条：切角深色仪表底板、双层硬边框、类型几何铭牌、分段填充、像素高光/阴影及纹理；HP 赤红、MP 青蓝、魔法护盾奥术蓝、物理护盾冷银。保留 `CombatHUD` 的现有值更新与护盾淡入淡出。`combat_hud_test.gd` 64/64 通过，视觉预览 `reports/ui_preview/resource_bars_1280x720.png`。
- 用户指出符文之语图鉴仍有大量视觉复用，现将“每个 `word_id` 独立完整纹理”设为硬规则：内场背景刻纹、边框刻度/缺口、中央签名字形、侧印/冠饰至少四层受 ID 稳定种子影响；全部 33 枚完整 RGBA 数据两两不同；术语表已展开记录符文、符文之语和资源计量仪表条完整绘制/验收规范。
- 06:00 架构复审确认上轮四项 P0 未修：Android 仍 `forward_plus`、库存常量悬空、地牢 11 处 `GameState.current_player`、攻击节奏分叉；进一步确认冷却有 CombatEngine/PlayerCombatRuntime/SessionRoot 三套，文档已决定 `damage_mult` 仍未统一进入结算。PerformanceBudget 只改 3D render scale 且无人订阅，export presets 仅 Web。报告 `reports/architecture_review_2026-08-02_hourly.md`。
- 实现固定法术组合与图标全链路：debug 构建打开酒馆装备仓库时自动补齐全部 50 种符文各 9 枚；`spell_recipe_data.gd` 扩为 33 个唯一有序固定配方，每项包含明确 `imagery`；新增 `data/spell_glyphs.gd`，按具体意象绘制独立 128×128 像素卡面并接入 `spell_interface.gd`。16/16 专项测试通过，图鉴 `reports/ui_preview/spell_codex_128px.png`。
- 06:45 只读架构审查报告 `reports/architecture_review_2026-08-02-0645.md`：既有 P0 未关闭；新确认法术仅完成配方/UI/图标，33 个配方未连接联机施放权威；SessionRoot 未从权威装备构建完整 AttackInput。未修改业务代码。
- 新增 `SpellRuntime` 与 `PixelSpellFx`：SpellRuntime 生成带 effect_plan/visual_event 的结构化计划，PixelSpellFx 仅做硬边视觉；专项测试通过，但玩家输入、ProjectileService 和服务端命令仍未接入，不能宣称法术完整可玩。
- 09:35 再次完成只读架构审查，报告 `reports/architecture_review_2026-08-02-hourly.md`。Godot headless editor 扫描通过；combat 45/45、spell runtime 4/4、performance 4/4；session 62/62 但 4 orphan/退出101，performance 进程报 31 ObjectDB/13资源泄漏。确认法术没有 CMD_CAST_SPELL/SessionRoot 入口，攻击仍是三套冷却，服务器 AttackInput 未完整取权威装备，库存旧常量、Android renderer、桌面/Android 导出预设和多人 current_player 绑定仍未收口。未改业务代码。
- 接通法术主链：`PlayerSpellCaster` + Player 数字键1–5选槽/C施法，CombatHUD同步界面选槽；`SpellAuthority` 执行治疗/护盾/位移/增益并接入 ProjectileService；联机新增 CMD_CAST_SPELL/EVT_SPELL_RESOLVED，客户端只发槽位，SessionRoot 服务端重解析。端到端专项 11/11、0 orphan。
- 新增 `SpellWorldExecutor`：消费 ray/area/ground/summon world_request，提供射线查询、周期 Area3D、有限时召唤节点、24个场/8个召唤预算与生命周期清理；SpellAuthority 已接线。PlayerContext 新增 per-peer SpellLoadout/SpellRuntime/Mana，SessionRoot 去除法术解析对全局 GameState 依赖并扣除 per-peer 法力。世界执行器复验 8/8、0 orphan/错误；剩余风险是具体召唤AI/地形阻挡、per-peer重连快照及服务端完整冷却 commit。报告更新 `reports/spell_system_architecture_review_2026-08-02.md`。
- 12:40 增量架构复审输出 `reports/architecture_review_2026-08-02-1240.md`。新发现 P0：`rpc_client_spawn` 接受客户端 `save_state` 并写入服务端权威材料/装备，绕过命令反作弊且违反服务器唯一存档设计。法术网络入口虽已新增，但正常 spawn 不载入法术装配，服务端不走 SpellRuntime 冷却/资格/commit、不执行 SpellAuthority，客户端桥接不消费事件，故仍非完整联机玩法。解析扫描通过；法术/协议/安全专项全绿；session 62/62 但 4 orphan、31 ObjectDB/13资源泄漏。
- 继续推进世界实体：SpellWorldExecutor 细化 imagery 行为，地面石墙/地震/岩刺创建 StaticBody3D 阻挡体，召唤构造体按 1 秒搜索 7m 敌人并调用受击入口；SpellRuntime 新增权威冷却检查、剩余时间与序列化，PlayerContext 新增 spell_state 序列化/恢复，SessionRoot 施法扣法力前校验 per-peer cooldown。完成测试：状态持久化 2/2、世界/端到端 8/8、0 orphan；最终报告更新 `reports/spell_system_architecture_review_2026-08-02.md`。
- 闭合联机法术最终链路：SessionRoot 生成 effect_plan、调用 SpellAuthority，广播 mana_remaining/cooldown_ms/authority_execution；MultiplayerSceneBridge 消费 EVT_SPELL_RESOLVED，仅播放 PixelSpellFx 并同步本地法力；重连快照写入/恢复 spell_state。另修复联机安全 P0：远端 rpc_client_spawn/send_spawn 丢弃客户端 save_state，仅房主本地可信路径可注入存档。最终专项 13/13 通过，0 orphan；`git diff --check` 通过。
- 测试并修复法杖/魔导书交互闭环：右键按住打开法术界面，界面显示时 Player 不再抢回左键鼠标捕获；固定符文配方完成后同步选中槽。staff/grimoire 左键进入 800ms 蓄力，只有 charge_ratio>=1.0 才调用当前槽法术；未满蓄力不进 SpellRuntime、不扣法力，非施法媒介即使蓄满也拒绝。专项 24/24、0 orphan，报告 `reports/spell_focus_flow_test_2026-08-02.md`。
- 19:52 增量架构复审输出 `reports/architecture_review_2026-08-02-1952.md`。确认远端 spawn `save_state` 注入已关闭且专项 3/3；但攻击仍信客户端 `attack_type`、服务端 AttackInput/冷却/流派倍率未统一，法术仍可能在 caster 未绑定时先扣资源并假成功，真实多人入口不加载 spell_state 且服务端不校验施法资格。Android renderer、桌面/Android presets、库存旧常量、性能分档消费者与 orphan 门禁仍未关闭。Godot 全项目扫描退出0；session 62/62但4 orphan、33 ObjectDB/14资源泄漏。
- 21:13 继续完成全项目架构审查收口，形成 `outputs/project_architecture_review_2026-08-02.md`。当前源码图谱为 783 个首方 GDScript、31 个 autoload、1422 条依赖边；报告按 P0/P1/P2 给出直接证据和整改路线。新增确认服务器移动仍是无碰撞坐标积分，可穿墙；当前工作树约 1926 个脏路径，不能视为可复现发布基线。
- 22:19 增量架构复审输出 `outputs/architecture_review_2026-08-02-2219.md`。新增 P0：真实大厅未传唯一身份，所有客户端默认使用 `player_guid="client"`；服务器不拒绝重复 GUID，而重连索引和结算账本都以 GUID 为键，可造成身份覆盖、错误重连和跨玩家串账。移动/攻击/法术/装备/库存/Android/presets/分档/orphan 旧阻断均未关闭。SessionRoot 62/62 但4 orphan、33 ObjectDB/14资源泄漏；移动12/12、法术网络4/4、性能4/4通过，性能测试报告清理旧目录失败但仍退出0。
- 21:19 增量架构复审输出 `outputs/architecture_review_2026-08-02-2119.md`。新增最高风险：服务器每收到一条合法 CMD_INPUT 就按固定 1/30 秒积分，没有 per-peer 消费频率上限，客户端可通过高频 RPC 加速，且仍无碰撞可穿墙；另确认装备命令不校验物品类型/槽位兼容、SpellAuthority 静态共享世界执行器会跨会话污染。既有攻击上下文/冷却、法术事务与资格、库存旧常量、Android renderer/presets、性能分档和 orphan 门禁仍未关闭。SessionRoot 62/62 但4 orphan、33 ObjectDB/14资源泄漏；移动12/12、法术4+4、spawn安全3、性能4均通过。
- 23:39 增量架构复审输出 `outputs/architecture_review_2026-08-02-2339.md`。确认旧 P0 大幅收口：spawn 注入、客户端 attack_type、攻击上下文/冷却、法术 caster/资格、listen-server 碰撞移动、Windows/Android presets 与 mobile renderer 已改善。当前仍有四个 P0：大厅所有远端默认共用 GUID `client` 且服务器不拒重复；专用服务器无碰撞且每 RPC 固定积分可穿墙/加速；联机法术只打场景 Node、不写回 SessionRoot `_entities`，可能扣资源播 FX 但敌人 HP 不变；装备命令不校验类别/槽位兼容。SessionRoot 63/63 但4 orphan退出101；其余专项58例全绿。