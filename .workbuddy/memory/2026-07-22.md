# 2026-07-22 工作日志

## Git：清理临时文件 + 完整提交 + 推送
- 用户要求清理临时文件后将项目改动完整提交并推送。
- 澄清：临时文件（带扩展名的 scratch 脚本/日志）与无关文件夹应加入 `.gitignore`（不删除本地文件）。
- `.gitignore` 新增：根 scratch `_*.py/_*.bat/*_tmp.py`、`probe_*.txt`、`*.log`、`*.tmp/*.TMP`、`project.godot~*`、`gdscript_codemap.json`；`__pycache__/`；`tmp/`、`tests/tmp/`；`reports/*.log|*.txt|*.xml`（保留 `reports/report_*/` 已提交的 HTML 测试报告）。
- 发现并修复：`**/__pycache__/` 在 git 2.53 下对嵌套目录不匹配，改用 `__pycache__/`（无前导斜杠即匹配任意深度）；`tools/__pycache__/*.pyc` 与 `scenes/characters/player/*.tscn*.tmp` 此前被误提交，本次一并 staged 为删除（保留本地、已忽略）。
- 提交 `c4b6e658`：4169 文件（660 删除 + 3509 新增/修改），含体素资产重做、gdUnit 测试套件、voxel 模型生成器与工具、设计文档、AGENTS.md。
- 环境注意：Bash 工具的 PortableGit 路径损坏，全程用 PowerShell 调 `D:\Git\cmd\git.exe`；push 走 Git Credential Manager 缓存凭据。

## docs：文档整理 + 冲突/细化分析
- 移除临时/无用文件至 `docs/_archive/`（保留可恢复，非硬删）：`agents/`(通用脚手架模板) / `gdscript_codemap.json`(根目录同名生成物重复副本) / `CODEMAP.md`(派生摘要且统计漂移) / `22-被动技能重设计`(被 `23`「全部被动无副作用」裁定推翻) / `mapgen_vs_barony.md`(结论已并入 04/set_piece)。
- 保留现状快照 `fp_weapon_pose.md` / `weapon_swing_animation.md`（供 task.md 动画优化参考）。
- 产出 `docs/README.md`：文档地图(按主题分组+状态) + 冲突清单 + P0/P1/P2 细化优先级 + 建议目录结构。
- 关键冲突：①被动体系 19/20/21/22/23/30 分裂，22被23推翻、B类机制数 9/11 不一、30游离主表、23-C组空白；②装备四方冲突 06(不可洗练) vs 32(鉴定/洗练) vs 34(材质随机) vs 33，层级模型互不兼容；③流派 31 自相矛盾(严禁击退 vs 盾击击退)；④战斗 05/16 仍写 %hit(75%) 模型，与实现「命中恒true+确定性均值」脱节，须重写。

## 战斗文档 05 重写（对齐实现）
- 重写 `docs/05-战斗系统.md`：命中改为 hitbox 接触即命中（删 %hit 投骰、删 hit_bonus/armor_evade/shield_block）；伤害改骰面确定性均值 `dice_count*(sides+1)/2`（删伤害投骰 RNG）；暴击保留为唯一概率放大器（神射手+10%暴击、穿透×1.12 仍生效）。
- 闪避/格挡从"百分比对抗"转为非 RNG 主动防御：闪避=主动位移 i-frame（不在 hitbox 内则不生效），格挡=持盾主动架势期固定减伤。
- 修正结构错乱：删除错位混在 §2 下的"## 2.3 装备品质磨损"块（反依赖未定稿的 32）；将 §4.1–§4.8 归到正确的 `## 4. 伤害结算算法（六阶段）` 下；修 §4.3 公式、§4.4 侧击（去命中率）、§4.5 盾牌格挡（去 1D100 对抗）。
- 修悬空引用：删已删 doc99 引用；`doc21 §5` 投掷被动改为指向 21 §2(投掷精通)/§3(投掷回收)。
- 同步 `docs/16-技术架构与代码设计.md` §4.1 CombatCalculator 示例：RollHitCheck(75%基础+闪避对抗)→ResolveHit(hitboxOverlap && !i-frame)；RollDice→DiceMean(确定性均值)；ICombatant.EvadeRate→DodgePerformance（不再削减命中率）。

## 装备方案唯一化（合并 06/32/33/34）
- 重写 `docs/06-装备系统.md` 为唯一权威总案：06 作基座（武器/防具类型 + §2 基础大表），32→磨损层（5 级品质阶梯+鉴定/诅咒/赐福），33→掉落层（实体真实掉落+深度阶位锁 Zone0-5 + 14正/10负/8后缀微数值词缀库 2%-5%），34→生成层（材质 6 × 稀有度 6 决定词缀插槽/UI 色/面板系数）。32/33/34 移入 `_archive/`。
- 关键裁定：① 随机只发生在生成（掉落/赠予）瞬间，成形即固定、不可洗练/重铸（调和 06 不可洗练 与 34 随机生成）；② 磨损用 32 的 5 级品质阶梯概率退阶，**废除 06 原点数耐久度**；③ 词缀库用 33 的微数值暴击口径（**废除 06 原 §3 的 6 正+6 负大数值及命中率/闪避率词缀**，因命中已恒真）；④ §2 基础表"命中率/闪避率"已按 05 改"暴击率/闪避性能档位"，骰面改确定性均值标注。
- 同步 `docs/README.md`：冲突点 ②(装备四方)、④(05/16) 标记已解决；P0 #1/#2 标记完成。
- 残留待拍板（记于 06 §9）：基础伤害三重缩放（§2 三阶 + §3 材质% + §3.2 稀有度系数）是否过强；诅咒词缀与诅咒状态锁定是否同一 `is_cursed` 逻辑。

## 根目录一次性脚本清理
- 用户要求删除「一次性脚本」：按此前分类，指 A 类 Python 数据修复/扫描脚本（下划线/`_tmp` 后缀）。
- 永久删除根目录 7 个：`_multiply_durability.py`、`_restore_refs.py`、`_scan_final.py`、`_scan_refs.py`、`_tone_probe.py`、`gen_csv_tmp.py`、`localization_scan_tmp.py`。均为已 gitignore 的未跟踪 scratch 文件，删除不影响版本库。
- 未删 B 类测试/构建驱动（bat/ps1：`_run_diag.bat`/`_run_test.bat`/`rebuild_cache.bat`/`run_ci.ps1`/`run_dungeon_tests.ps1`/`run_full_tests.ps1`），属可复用 CI/测试门禁，非一次性。

## 装备掉落/生成时机澄清（06 §4.1）
- 用户明确：掉落**仅掉落怪物持有的装备**；装备实例在**怪物与宝箱生成的同一瞬间**由生成器定型（怪物/宝箱"持有"该实例），掉落=把已生成实例转移给玩家，绝不在死亡/开启时二次随机。宝箱同生生成、无凭空爆装。已写入 06 §4.1 核心规则框。
- 二次澄清（装备≠仅武器）：掉落的是**完整配装（武器+防具+副手/盾+辅助挂件全槽位）**，不是只掉武器；**所有人形怪物**生成时均拥有正常的**装备槽位与武器槽位**，产出其完整配装并持有；击杀掉落其持有的全部装备（非仅武器）。已改 06 §4.1 核心框与"人形怪物满槽位生成"条。
- **三次澄清（概率掉落 + 槽位可空）**：掉落是**概率事件**，但只能从怪物**当前穿戴**的装备中随机抽取；怪物**仅有槽位、未必每个槽位都填有装备**（空槽位不贡献掉落）。据此把"随机"拆成两层写进 06：① 属性随机封死在生成瞬间（成形即固定）；② 掉落随机是死亡/开启时的独立概率事件（是否掉、掉哪件），只对已生成实例做概率抽取，不修改属性、不凭空生成。已改 §0 硬规则 2、§3 生成句、§4.1 核心框+"人形怪物拥有完整槽位但未必填满"条+"无凭空爆装"条、§9 决议 4。

## 子目录临时文件清理 + 事故恢复
- 检查子目录 scratch/运行时垃圾：删除了未跟踪的 `tmp/`、`tools/__pycache__/`、`.sandbox_appdata/`、`.sandbox_home/`、`Godot/`、`core_test_log.txt`，及 `reports/` 下未跟踪的 `*.log/*.txt/*.xml`（保留已提交的 `reports/report_*/` HTML）。
- `.godata/` 与 `.godot_headless/` 内含 3 个被误提交的 Godot 运行时文件（shader_cache `.d3d12`、editor_doc_cache `.res`、`editor_settings-4.7.tres`）→ `git rm --cached` 取消跟踪，并加入 `.gitignore`（`.godata/`、`.godot_headless/`、`.sandbox_appdata/`、`.sandbox_home/`、Godot/`）。
- **事故**：删除脚本的 `IsTracked` 用反斜杠路径比对 `git ls-files`（正斜杠）恒 false，误将已跟踪的 `reports/report_*/results.xml` 等从磁盘删掉，`git add -A` 又暂存了这些删除。恢复：`git reset -q HEAD` + `git restore --worktree -- reports/`（从 HEAD 拉回），`reports/` 已恢复、无丢失。
- 提交 `489a4f55` 并推送（仅含 `.gitignore` + 3 个 Godot 文件取消跟踪，最小化）。
- **故意未提交**（属此前各任务的工作，非临时文件）：9 个归档到 `docs/_archive/` 的文档删除、`docs/05`、`assets/`、`proficiency_*` 图标等约 34 处改动、`reports/report_1146..1150/` 5 个新未跟踪报告。
- **待用户拍板**：`tools/probe_*.gd`（4 个调试脚本 + 配套 .tscn/.uid，已跟踪源码）是否删除。

## reports/ 整个文件夹加忽略
- 用户要求整目录忽略（不是之前只忽略 logs/txt/xml）。
- `.gitignore`：将 `reports/*.log|*.txt|*.xml` 三条改为 `reports/`。
- `git rm --cached -r reports/` 取消跟踪全部 1694 个已提交的测试报告文件（保留磁盘、已忽略）。
- 提交 `000fbac0` 并推送 `489a4f55..000fbac0`；暂存集严格为 `.gitignore` + 1694 删除，未夹带其他改动。本地与远端同步。
- 此前"保留 reports/report_*/ 已提交 HTML"的策略作废——整目录不再入库。

## 被动表现状核查（仅诊断，未动手）
- 六份文档：19=符文乘数层(B类符文池=9正典名,放大器)；20=机制树骨架层(武器层10机制+流派层14机制,被放大对象之一)；21=统一被动总表(工作主表:A组18属性里程碑+B组11机制被动+C组12数值被动)；22=已归档(取舍对等/sustain有代价,被23推翻)；23=最新裁定(全无副作用,r rename壳:A组18新名+Duo6+C组空白);30=扩展4项(坚韧/汲取/决斗者/奥法之剑)游离主表外。
- **"9 vs 11"是 README 误读两类 B**：9=19§11 的 B 类**符文**(放大池)；11=21§0 B 的 B 类**机制被动**(被放大对象)。二者是"放大器 vs 被放大"不同范畴,无真矛盾。真残留:机制被动全集被拆在 20(武器/流派机制:riposte/cleave/execute/stagger/reach/pierce/reload_shot/lifedrain/盾击…)与 21(操作强化:charge/air_dash/ram…)两处,都被同一 9 符文池放大,缺"机制被动唯一总表"。
- **doc21 内部数字漂移(真bug)**:§0 C 标题与表=12 项数值被动;但 §5.1(line297)"13数值+11机制=24"、§9.1(line364)"数值类13条(10武器+2盾+1投掷=13)" 与 §0 C 实际表(11武器+1盾=12)对不上 → 以 §0 C 表 12 为准,"13"是笔误。
- **doc23 A组是重命名空壳**:21§0 A 与 20§4 已有 18 属性里程碑具体效果(震退=15%击退1m,且与代码 skill_data.gd ATTR_MILESTONES 对齐);23 A组用新工作名(震荡冲击/磐石姿态/巨化…)重列同一18个但**全部⬜待定、效果单元格为空**,仍沿用22的sustain/形态词汇 → 造成"18里程碑叫什么/效果写哪"双源。23的"无副作用"是政策裁定,21/20的实际被动已纯增益与之对齐。
- **30 游离 + 23 C组空白**:30 的4项未并入21/23;23 C组(12武器线×3级=36)标注"待逐条展开"⬜。
- 结论:被动表并非实质矛盾,而是**碎片化+部分冗余+数字漂移**;22已归档、23为政策壳、21为工作主表。建议以21为唯一主表:回填23无副作用裁定+新名(作别名)、用21现有效果填满23 A空壳、并入30的4项、补"机制被动全集(含20武器/流派机制)"节、修21的12/13漂移、展开23 C组或显式延后。待用户拍板是否动手。

## 被动表收口合并（已动手完成）
- 以 21 为唯一权威主表执行合并:① §0 A 头部加「对齐 23 裁定:全部被动统一无副作用」政策声明 + 命名权威(代码名=skill_data.gd ATTR_MILESTONES);② §0 A 加 23 展示别名对照表(10 个与 21 同里程碑)+ 显式标注 23 另有 6 个重设计名(磐石姿态/巨化/千手/相位/元素亲和/不朽,效果在已归档 22 且含被推翻的"代价")**不并入**主表命名,留作重设计候选待拍板;③ 新增"机制被动全集"说明:B组11操作强化 + 20 武器/流派机制,均被 19§11 的 9 符文池放大(纠正 README "9 vs 11"误读=放大器 vs 被放大对象);④ 并入 30 的 4 项为 §0 D 组 + §3.5 详表(坚韧/汲取/决斗者/奥法之剑);⑤ 修 21 数字漂移:§0 C=12(11武器+1盾)、§5.1"24→23(12数值+11机制)"、§9.1"13→12 条"。
- README 同步:冲突①标记✅已收口(纠正 9 vs 11 误读)、05/06 过期状态修正为✅、P0#3 标记✅完成、doc 地图 21=唯一权威主表/23=政策权威(6重设计名待拍板,C组延后至装备精通树)/30=已并入。
- **唯一待拍板点**:23 A 组 6 个重设计名(磐石姿态等)与 21 现有效果分歧——采用 21 现有效果则沿用代码名;采用 23/22 重设计则须在"无副作用"框架下重写效果后再改名。未擅自动该 6 项。

## 被动文档收窄为 4 项定稿（用户指令）
- 用户:"删除整个被动技能文档,仅保留已经决定的流派专精被动和坚韧汲取。其余全部移除"。
- 解读:"流派专精被动"=全 docs 中唯一武器流派限定被动=doc30 的 **决斗者/奥法之剑**(均 `ONE_HAND_SWORD` 限定);加通用 **坚韧/汲取**。其余(18 属性里程碑/11 机制操作强化/12 武器数值/23 别名+重设计壳/符文放大层/Duo)全部移除。
- 执行:① 重写 `21` 为极简权威清单(仅 4 项:一、流派专精=决斗者/奥法之剑;二、通用=坚韧/汲取),含 JSON 配置;② 归档 `30-扩展被动技能策划案.md`→`_archive/`(4 项已并入 21);③ README 同步:doc 表 21=极简权威清单/23=设计参考已非权威/30=已归档;冲突①改写为 4 项收口;P0#3、line47(去 21 A组依赖)、line113(21 B组已移除)、line159(30 归档)一并修正。
- **保留未动**:`20`(被动机制树)/`23`(效果记录)作为设计参考仍留在 docs(非被动权威);其内"doc21 §4"等锚点现已失效(待用户决定是否归档 20/23 或定稿符文放大系)。
- 删除动作用了 PowerShell Move-Item(Bash 的 PortableGit 仍坏)。

## 被动文档纠错：流派专精=7 流派×2=14 项（非仅 2）
- 用户纠正:"流派专精被动应已经覆盖所有流派x2,不仅俩个"。我前次误把"流派专精"缩成 doc30 的 2 个单手剑限定(决斗者/奥法之剑)。
- **正解**:流派专精被动= doc31 §一「7 大手部握持流派核心机制纯被动设计」= **14 项**(7 流派 × 2):单手(决斗者/奥法之剑)、持盾(盾击/折射)、双手(蓄势/重型挥舞)、双持(交错挥砍/十字返)、徒手(暴风骤雨/过肩摔)、远程(看破弱点/贯穿)、法系(奥术护盾/元素环)。school ID 用 doc31 的 `ONE_HAND`/`ONE_HAND_SHIELD`/`TWO_HAND`/`DUAL_WIELD`/`UNARMED`/`RANGED`/`SPELL`(注意:doc30 曾用 `ONE_HAND_SWORD`,与 doc31 不一致,以 31 为准)。
- 执行:重写 `21`= 流派专精 14 项(按 7 流派分组,效果/触发引 doc31 §一)+ 通用(坚韧/汲取 JSON);`30` 的 4 项已并入但 决斗者/奥法之剑 现在归属单手流派 2 项之一。README 同步 line72/95/135 改为 16 项(14+2)。
- **刻意排除**:doc31 §二「10 大武器兵器大类核心机制」(序号15-34,20 项)属"武器大类被动"非"流派专精",按用户"其余全部移除"不进 21。
- 教训:凡"流派专精"= doc31 §一全量 14,勿再缩成单手剑 2 项。

## 数值体系文档构建（收集→落档）
- 用户:"构建数值体系文档,先收集项目内已决定信息"。
- 执行:① 用 Explore 子代理彻底勘察代码+文档 8 大类数值(属性/战斗/装备/被动符文/敌人/成长/经济酿酒/性能),带 file:line 引用;② 抽验核心公式(dice_avg / crit_rate / crit_mult / final_def / 派生属性 / 材质倍率)确认无误;③ 新建 `docs/数值体系.md` 为权威数值参考,每条标【代码】/【文档】/【冲突】+来源;④ README 登记(§二 核心设计表 + §四 P2#12 指向其 §9 九项缺口)。
- 文档结构:§0 基调 / §1 属性(派生公式+18 里程碑+熟练度) / §2 战斗公式(命中/骰子/暴击/防御/风格 STYLE_META/动量冲击/i-frame) / §3 装备(骰子/材质倍率/材质区锁/稀有度/词缀/品质) / §4 被动符文(16 项+7 符文槽) / §5 敌人(精英倍率/AI 常量/区域倍率/基础 HP) / §6 成长 / §7 经济酿酒(金币/声望6档/酿造/酒馆升级成本) / §8 性能(12 灯/30Hz/18m/4 帧) / §9 九项待定缺口。
- 抽验确认的硬常量:MaxHP=100+CON*10+LV*5;def=armor+CON;carry=50+CON*2;crit=5+PER*0.5;dice_avg=count*(sides+1)/2;crit_mult=1.5+PER*0.01(下限1.1);材质倍率 wood0.9/iron1.0/steel1.05/meteoric1.10/mithril1.15/adamantite1.20。
- 共发现 9 项待定(§9):翻滚 i-frame 缺失、敌人无独立伤害字段、每级固定属性点缺失、材质命名陨铁vs玄铁冲突、B 类符文条目待定、遗留 brewing_materials.json 未接入、武器学派键不统一(10枚举vs7聚合)、酒馆升级成本未代码常量化、"34"来源无引用。
- 注意:Explore 报告称 docs/06 用"陨铁"而代码翻译 meteoric=玄铁,已在文档标【冲突】#6,建议以代码为准对齐。

## 数值体系.md 删除重做→战斗数值体系.md
- 用户:"删除数值体系.md重新收集,改为战斗数值体系"。
- 执行:① PowerShell Move-Item 将原 `docs/数值体系.md` 移入 `docs/_archive/数值体系_废弃_全量版.md`(项目惯例:文档移除走归档非硬删);② 重新聚焦收集(Explore 子代理 combat-only pass),补全前次不足的战斗专属细节:状态效果(眩晕0.5s/击退由技能专用:踢击3.0m·冲撞6.0m·双手+4.0)、物理撞击(damage_pct=0.06+ratio*0.24,满速=30%maxHP,冷却350ms,体型small1.10/large0.80/huge0.60,阶级boss0.65/elite0.85)、动量cap(10/0.3/0.8/0.15)、action_skills 五技能数值(踢击/冲撞/抓取投掷/滑铲0.5s iframes/战术滑步0.3s dodge)、敌人攻击(全属性10/def=0+con10/走武器骰子)、投掷撞击固定(6伤/5.0击退)、联机(30Hz/1-30tick/0.4s攻击CD/cos75°=150°扇区/近战2.5m·远程18m/端口54321/4人/光影12/AI18m)、A类7符文数值、B类9符文仅文档。
- 新建 `docs/战斗数值体系.md`:§0 基调 / §1 属性→战斗派生(公式+里程碑战斗加成) / §2 伤害结算(STYLE_META/平砍/暴击/朝向/防御/状态/撞击/动量) / §3 攻击输入与命中盒 / §4 动作技能与i-frame / §5 装备战斗数值(材质/武器骰子/盾/词缀/稀有度品质诅咒) / §6 被动符文 / §7 敌人 / §8 联机时序 / §9 八项战斗缺口。
- README 同步:§二 "数值"行→"战斗数值"(`战斗数值体系.md`);P2#12 改为指向其 §9 八项战斗缺口。

## §8 联机噪声剔除（战斗数值体系.md）
- 用户批评:"联机时序常量跟战斗数值有毛关系?"——正确。§8 原有 9 行里 5 行是联机/性能噪声(快照30Hz/服务器tick 1-30/端口54321/人数4/光源12/AI18m),与战斗数值无关。
- 已重做 §8 为"战斗时序与几何":仅留 4 个真战斗值(攻击冷却0.4s/攻击扇区cos75°≈150°/攻击射程近战2.5m·远程法术18m/移动基准4.0×1.6),并标注这 4 个仍属"代码占位·无文档·待定";联机/性能噪声移出本档、归 16-技术架构。文档头用途/范围两行同步去掉"联机同步"措辞。
- 仍待办:整篇【代码·已决定】须按"有文档=决定/仅代码=占位符"规则降级重标(上次被打断)。

## 关键纠正：源码战斗数值=占位符，文档才是权威
- 用户:"源码公式和算法为占位符。" → 我此前在 `战斗数值体系.md` 把 gd 常量标 `【代码·已决定】` 是错的定性。
- 每一条(眩晕0.5s/踢击3.0m/冲撞6.0m/双手+4.0/格挡移除盾只加防/敌人con=10/投掷撞击6伤5.0击退)都确有 file:line(damage_resolver.gd / action_skills.gd / combat_bridge.gd / enemy.gd),出处就是源码——但皆为占位符。
- **反转**:文档(05/06/21/31)是权威,代码是与文档脱节的占位脚手架,应被对齐到文档。我原 `【冲突】#6`(玄铁vs陨铁)建议"以代码为准"须反过来→改代码迁就文档。
- 行动:待用户定 `战斗数值体系.md` 处理方式(就地降级所有【代码】为占位符 / 重做只收文档决定值 / 加占位符横幅)。已先把此约定写入 MEMORY.md 战斗节。
- 范围收窄:经济/酒馆/酿酒/非战斗成长已移出(仅留 §1 属性派生、§5 装备、§7 敌人、§8 联机这些战斗相关)。
