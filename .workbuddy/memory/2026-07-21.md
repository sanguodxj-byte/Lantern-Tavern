# 2026-07-21

## 地牢性能优化实现（按排查报告 5 项全部落地）
- 流控差集增量：dungeon_streaming_controller.gd 灯光/视觉/地形 chunk 改差集更新，去掉 O(N) 全扫 hide-all。
- 火把粒子/音频联动：视觉节点隐藏时递归暂停 GPU/CPUParticles3D(emitting) 与 AudioStreamPlayer3D(stream_paused)，注册即暂停。
- 敌人 LOS 节流：enemy.gd has_line_of_sight_to 加 0.2s 缓存(_los_cache_timer)。
- P0 敌人分帧实例化：DungeonSpawner 新增 build_enemy_spawn_plan / instantiate_enemy_descriptor，spawn_enemies_from_layout 加 batched 参数；DungeonRuntime.spawn_enemies 取计划按帧批量(4/帧)实例化，无树回退同步。
- P1 实体同步 30Hz 节流：network_manager server_update_entity 在有真实 peer 时把 EVT_ENTITY_SNAPSHOT 入 _entity_update_buffer(按 entity_id 合并)，随 _flush_snapshots 30Hz 下发；spawn/despawn 仍即时可靠。
- 修正：MEMORY.md 灯光预算 28→12（代码实际值）。
- 验证：headless --script 解析 5 文件，修复 Particles3D 基类不可作类型名的错误（改用 GPUParticles3D/CPUParticles3D）。其余"identifier not found"为 --script 模式 autoload 不全误报。

## 地牢性能第二轮排查
- 发现首轮"火把音频暂停"不完整：火把在 `_spawn_torch_on_wall` 只注册为 physics 节点(`streamed_physics_nodes`)，不走可视节点，故首轮的可视路径暂停对其无效。补漏：在 `_set_visual_root_active` 对物理注册根也调 `_apply_visual_side_effects`，火把离激活半径即暂停音频/粒子。已落地。
- 新热点（待实施）：P-A 生成仍单帧（build+同步烤 navmesh `_build_navigation_mesh:720`）；P-B navmesh 雷群(`enemy_state_moving` 同批 150ms 对齐重算 A*)；P-C 物理激活半径~36m 与 imposter LOD 18m 失衡；P-D 火把/装饰未 MultiMesh 合批(draw call 高)；P-E shader 首编 hitch 无预热。
- 澄清：`network_manager.tick()` 单人 early-return、`_run_detection` 不存在、`exploration_pressure` 每分钟几次→均非热点。
- 报告：docs/地牢性能排查_第二轮.md

## 地牢性能第三轮（继续优化）实施 P-A / P-B / P-C
- P-B navmesh 雷群抖动（已生效）：enemy_state_moving.gd 加 `PATH_UPDATE_JITTER_MS:=50` + 每实例 `_path_update_interval_ms := PATH_UPDATE_INTERVAL_MS + randi_range(0,50)`，初始相位 `-randi_range(0,150)`；追击/巡逻两处节流比较改用 `_path_update_interval_ms`。错开同批敌人 set_target_position(A* 整图) 触发帧。
- P-A 生成/导航异步烘焙（代码就绪、默认关闭）：dungeon_scene_builder._build_navigation_mesh 加 `ENABLE_ASYNC_NAVMESH_BAKE:=false`（默认同步，生产已知可用）。开启则 `NavigationServer3D.bake_from_source_geometry_data_async` 后台烤（API 缺失回退同步）。perf 测试靠前缀子串仍通过。
  - 关闭原因：本环境仅 headless，导航烘焙文档不稳定（偶发 crash；--script 下异步回调不触发致探针挂起），无法验证异步能把多边形回填进 NavigationRegion3D；若失败敌人永久无法寻路=灾难。待窗口化冒烟测试后再置 true。
- P-C AI 半径与 LOD 对齐（已生效）：enemy.gd 加 `AI_SIM_RADIUS_M:=18.0`（=ENEMY_IMPOSTER_LOD_DISTANCE 边界）+ `is_ai_active()`（已交战/暗蚀强制/玩家≤18m→true；远距未交战→false）；enemy_state_moving._physics_process 开头门控：`is_ai_active()` 否时仅清零水平速度+process_movement 保持静止，跳过追击/巡逻 A* 与导航查询。削减 18–36m 替身带空算。
- 验证：三个文件 headless 解析校验 ALL_OK。临时 tools/_parse_check.gd 与 _nav_async_probe.gd 验证后已删。
- 剩余未动：P-D 火把/装饰 MultiMesh、P-E shader 预热(已有 shader_warmer+world.gd)、P-F/P-G 射线预算/fx 池化。
- 报告更新：docs/地牢性能排查_第二轮.md 新增「五、实施状态」。

