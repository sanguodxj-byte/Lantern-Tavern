# 2026-07-09

## 攻击怪物卡死（Request 4）— 根因与修复
- 链路：player_state_slashing._physics_process → _resolve_hitbox_overlaps → _resolve_enemy_hit → enemy.try_receive_hit_result → switch_state(HURT) → HURT._enter_tree 同步调用 transition_state(DYING) → EnemyStateDying._enter_tree 直接在物理步进内执行物理操作 → 引擎死锁/卡死。
- 确认：damage_resolver/combat_bridge/attr_panel/skill 链路无死循环；卡死来自 DYING._enter_tree 的物理操作在 _physics_process 期间运行（与 enemy.gd 踢击致死的注释完全吻合）。
- 修复：enemy_state_dying.gd 把布娃娃启动/碰撞关闭/掉落物搬进 call_deferred("_begin_death_effects")；headless 守卫布娃娃模拟（OS.has_feature("headless")）。
- 新增回归测试 tests/gdunit/enemy_dying_defer_test.gd。
- 注意：Godot headless stdout 切勿管道接 head（会假死）；APPDATA 用 Windows 路径 D:/godot_apdata；已记入 MEMORY.md。

## "进行检查" 复验 — 发现并修复两处 headless 稳定性问题
- **headless 检测的正确姿势（关键）**：探针实测 gdUnit4 headless 下：`OS.has_feature("headless")`=**false**（不可靠）；`--headless` 会被引擎从 `OS.get_cmdline_args()` **消费掉**（返回 `["-s","tests/gdunit4_runner.gd"]`，不含 --headless）→ 之前 `"--headless" in OS.get_cmdline_args()` 守卫全部失效；唯一可靠：`DisplayServer.get_name()=="headless"`（也可 `RenderingServer.get_rendering_device()==null`）。
- 修复①：enemy.gd `_build_imposter_texture` 与 enemy_state_dying.gd 死亡物理副作用守卫统一改用 `OS.has_feature("headless") or DisplayServer.get_name()=="headless"`（保留 has_feature 字符串以兼容 imposter 源码级测试 test_imposter_capture_skipped_in_headless）。dying 内抽出 `_is_headless()` 私有方法。
- 修复②：**headless 下同一进程重复实例化 skeleton-rig 敌人（rat 等，蒙皮 rig+子 Viewport）第 2 次必 signal 11 崩**（纯引擎崩，无 GDScript 栈；真机正常）。对策：把每个含多次 rat 实例化的死亡测试**合并为单只 rat 的完整流程测试**——enemy_dying_defer_test 合成 1 用例（延迟→物理帧后执行两阶段断言）；enemy_knockback_death_defer_test 合成 1 用例（LAUNCHED→飞行延迟→compute_death_data 撞墙方向→wants_launch 决策，全在一只 rat 上）。
- 复验全绿：enemy_dying_defer_test(1/1)、enemy_knockback_death_defer_test(1/1)、enemy_block_damage_test(17/17)、enemy_state_moving_test(9/9)、enemy_detection_test(16/16)、enemy_imposter_viewport_test(3/3)、enemy_imposter_lod_test(8/8)，无 signal 11。

## 文档 20-被动机制树（构筑骨架）— 双轨领悟被动树展开
- 用户选"先做1"：把双轨领悟被动树从占位展开成具体流派/阶位/机制授予表（build 骨架，风险2=B类同质化 的解药）。
- 新建 `docs/20-被动机制树（构筑骨架）.md`：§1 双轴被动树模型(流派机制轴×属性里程碑轴)；§2 10流派机制授予表——每流派 T3 授予唯一 weapon_mechanic(id/type/params)，10机制彼此不同(riposte/cleave/execute/stagger/reach/pierce/multishot/element_infuse/spell_echo/lifedrain)，B类符文在其上放大/升级；§2.1 参数草稿；§3 6属性里程碑轴(写实自doc15 §3.2)；§4 机制×B类符文增强映射；§5 消费管线(G1/G8)；§6 待决 D-T1~D-T5(回写 skill_data.gd 的 E/F/G/H/I T3 被动名、mechanic 路由等)；§7 文档互链。
- doc19 §11.2 加交叉引用指向 20（"19是乘数层，20是骨架层"）。
- 设计要点：10流派机制各自唯一且差异显著，避免B类退化为数字微调；element_infuse/spell_echo 的B类键(burn_*/repeat_count)已存在于 rune_data.gd(ember/echo)，其余机制需G8补路由。

## 文档20 v2 重构：武器层 × 流派层 双层模型（用户纠正）
- 用户纠正：流派和武器是**两层**，不可混淆。单手剑/战锤/长枪都可配盾→持盾流派(跨武器共享,机制=盾击shield_bash+持盾可用技)；不配盾的单手剑是另一个流派(迅捷剑术FENCING,机制=riposte/flurry)。原doc20把流派=武器媒介是错的。
- doc20 重写为双层：§1 双层模型(武器层×流派层,笛卡尔积组合)；§2 武器层(10武器,grip单手/双手,shield_compatible,固有机制)；§3 流派层(3.1跨武器共享=持盾流派SHIELD;3.2武器独占无盾流派=单手剑迅捷剑术/战锤狂暴重击/长枪游斗刺击;3.3合成机制总表);§4 属性里程碑轴;§5 机制×B类符文映射;§6 管线(G1/G8);§7 待决D-T1~D-T6(回写skill_data的grip/shield_compatible字段、流派条件授予、shield_allow_skill反向规则等)。
- doc19 §11.2 互链措辞同步改为"武器层×流派层双层模型"。
- 关键设计点：持盾是组合数最大的共享流派；build机制=武器固有+流派机制；合成后B类符文放大。shield_bash/riposte等需G8补路由。

## 文档20 v2.1 武器形态再修正（用户二次纠正）
- 用户纠正：战锤/长枪**无纯单手形态**，只有「配盾」或「双手」两种配置；**纯单手无盾仅单手剑一种**。原v2把它俩标成"单手可配盾✅"仍错。
- doc20 修正：§1 组合规则(单手剑=纯单手无盾/配盾；战锤长枪=配盾/双手)；§2 武器层表(战锤/长枪 grip改"配盾/双手(无纯单手)"，纯单手无盾仅单手剑)；§3.2 标题改"单手剑纯单手流派 & 战锤/长枪双手形态"，战锤/长枪非持盾形态=双手(非无盾)；§3.3 合成表"战锤×狂暴重击(双手)""长枪×游斗刺击(双手)""双手大剑(双手)""双手斧(双手)"；§7 D-T2 标已决议。

## 文档21-被动技能（数值类+机制类）— 新设计
- 用户要求设计被动技能，分两类：数值类(针对某种武器/盾的提升) + 机制类(强化已有操作逻辑)。机制类举例：左键按住蓄力、跳跃后半空向前二段跳、攻击冷却缩短、按住Shift奔跑撞敌向旁。
- 新建 `docs/21-被动技能（数值类与机制类）.md`：§1 分类总览(数值类低险/机制类高险_hook核心状态机)；§2 数值类(schema + 按武器/盾目录11条)；§3 机制类(操作清单Operation Inventory: 8操作#1蓄力#2空中二段跳#3急速#4撞敌 + 招架/残影/取消后摇/自动拾取, 含用户4例+hook)；§4 机制类×B类符文放大(闭环)；§5 解锁(双轨领悟阶梯映射)；§6 实现注记(机制类高险=AI排错成本焦点,需强类型+单测)；§7 待决D-P1~D-P4(同op叠加/跨武器/对齐代码状态机/cd_reduce归类)；§8 衔接15/19/20。
- 关系澄清：doc20的流派机制(riposte/cleave)=武器命中机制(流派层授予)；doc21机制类=操作强化(阶梯授予)；两者都是机制但来源/对象不同，B类符文统一放大。
- doc19 §11.2、doc20 §8 加交叉引用指向21。

## 文档21 机制类新增：完美格挡精通（用户补充已有机制+新被动）
- 用户补充已有机制(游戏现有)：完美格挡窗口0.3s；双手武器无盾、唯一免伤=完美格挡；持盾完美格挡不消耗盾牌耐久。
- 新增机制类被动 #5 完美格挡精通 perfect_block_mastery：触发完美格挡后(1)窗口0.3s→0.8s (2)下次攻击伤害+X%(buff标记,攻击结算读取)。替换原parry_window条目。
- doc21 改动：§3.2 加"已有机制基线"callout(0.3s/2h唯一免伤/持盾不耗耐久)；#5行改为perfect_block_mastery；§3.3 schema enhance枚举去parry_window加perfect_block_mastery；§4 B类符文映射行更新；§5 解锁(双手武器T2或持盾流派T2)；§7 新增D-P5(0.8s是否同时作用持盾/双手)、D-P6(下次攻击+X%数值与失效规则)。原parry_window引用已全部清除。
